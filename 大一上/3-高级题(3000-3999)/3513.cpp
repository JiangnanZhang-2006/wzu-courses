#include <bits/stdc++.h>

using namespace std;

int main() {
	int n;
	cin >> n;
	vector<pair<char, int>> arr(n);
	for (int i = 0; i < n; i++) {
		cin >> arr[i].first >> arr[i.second;
	}
	vector<bool> ans(n) 
	for (int i = 0; i < n; i++) {
		for (int j = i + 1; j < n; j++) {
			
		}
	}
}