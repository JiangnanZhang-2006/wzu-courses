#include <bits/stdc++.h>
using namespace std;

pair<c   concat(pair<int, int> a, pait<int, int> b, vector<int>& weights) {

}

int main() {
    size_t melon_len, link_len;
    if (!(cin >> melon_len >> link_len) || melon_len % 2 == 0) return 0;
    
    vector<int> weights(melon_len);
    
    for (size_t i = 0; i < melon_len; ++i) cin >> weights[i];

    vector<pair<int, int>> links(link_len);
}