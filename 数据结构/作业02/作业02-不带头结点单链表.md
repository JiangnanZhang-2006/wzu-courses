# 作业02 不带头结点单链表

> 来源：[课程题集](https://10-132-246-246.webvpn.wzu.edu.cn/course/7/volume/728/detail/)

## 目录

1. [3745-初始化空表](#3745-初始化空表)
2. [3746-尾插](#3746-尾插)
3. [3747-输出链表](#3747-输出链表)
4. [3748-头插](#3748-头插)
5. [3749-判空](#3749-判空)
6. [3750-获取长度](#3750-获取长度)
7. [3751-按位置取值](#3751-按位置取值)
8. [3752-按值查找](#3752-按值查找)
9. [3753-指定位置插入](#3753-指定位置插入)
10. [3754-删除指定位置](#3754-删除指定位置)
11. [3755-删除所有指定值](#3755-删除所有指定值)
12. [3756-逆置](#3756-逆置)
13. [3757-清空](#3757-清空)
14. [3758-销毁](#3758-销毁)
15. [3759-逆置区间](#3759-逆置区间)
16. [3760-删除最小值](#3760-删除最小值)
17. [3761-拆分链表](#3761-拆分链表)
18. [3762-删除区间值](#3762-删除区间值)
19. [3763-有序合并](#3763-有序合并)
20. [3764-去重](#3764-去重)
21. [3765-集合交集](#3765-集合交集)

---

## 3745-初始化空表

### 问题描述

本题要求你实现**不带头结点单链表**的初始化操作。

不带头结点单链表的特点：

* 不存在头结点，头指针直接指向第一个数据节点
* 空表时，头指针为 `NULL`
* 所有操作都需要考虑空表的特殊情况

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 初始化空表 —— **本题核心**

```c
void InitList(LinkList* pL);
```

**功能要求**：

* 将头指针置为 `NULL`，表示空表
* 不需要分配任何节点内存
* 参数为二级指针，用于修改头指针的值

**特殊情况处理**：

* 若 `pL == NULL`，直接返回，不做任何操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务，不允许使用数组或其他数据结构替代。
2. **不分配头结点**：不带头结点的链表初始化时不需要分配任何内存，只需将头指针置为 `NULL`。
3. **空表判断**：初始化后的空表满足 `*pL == NULL`。
4. **二级指针理解**：使用二级指针 `LinkList*` 是为了能够修改头指针本身的值。

---

### 【数据范围】

* 链表节点个数：不超过 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 初始化成功
  * 空表验证
  * 后续操作验证

---

### 【提交说明】

* **只需提交 1 个函数**：`InitList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
void InitList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 InitList 时，学生只提交：
void InitList(LinkList* pL) { ... }
*/
```

---

## 3746-尾插

### 问题描述

本题要求你实现**不带头结点单链表**的尾插操作。

尾插定义：

* 在链表末尾插入新节点
* 新节点成为最后一个数据节点
* 空表时，新节点成为第一个节点，需要修改头指针

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 尾插 —— **本题核心**

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表末尾插入元素 x
* 成功返回 `OK`

**失败条件**：

* `pL == NULL`：返回 `ERROR`
* 内存分配失败：返回 `ERROR`

**失败时**：不得修改链表

**特殊情况处理**：

* 空表时（`*pL == NULL`），新节点成为首节点，需要修改头指针

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **空表特殊处理**：
   * 若 `*pL == NULL`，新节点成为首节点
   * 设置 `*pL = newNode`

3. **非空表尾插**：
   * 从 `*pL` 开始遍历，找到尾节点（`tail->next == NULL`）
   * 设置 `tail->next = newNode`

4. **内存安全**：分配失败时返回 ERROR。

5. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 空表插入
  * 非空表插入

---

### 【提交说明】

* **只需提交 1 个函数**：`PushBack`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
Status PushBack(LinkList* pL, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 PushBack 时，学生只提交：
Status PushBack(LinkList* pL, ElementType x) { ... }
*/
```

---

## 3747-输出链表

### 问题描述

本题要求你实现**不带头结点单链表**的输出操作。

输出规则：

* 空表或 NULL：输出空行
* 非空表：输出所有元素，元素之间用**一个空格**分隔，**最后一个元素后无空格**，最后换行

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 输出链表 —— **本题核心**

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`（空表），输出空行（直接换行）
* 否则输出所有数据节点的值，格式：`e1 e2 e3 ... en\n`
* 元素之间用**一个空格**分隔
* **最后一个元素后无空格**，最后换行

**输出格式示例**：

```
1 2 3 4 5
```

（注意：5 后面**没有空格**，直接换行）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。
2. **输出格式严格**：元素间一个空格，最后一个元素后无空格。
3. **推荐实现**：使用标志变量 `first` 控制是否输出空格。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 空表
  * 单元素
  * 多元素

---

### 【提交说明】

* **只需提交 1 个函数**：`PrintList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
void PrintList(LinkList L);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 PrintList 时，学生只提交：
void PrintList(LinkList L) { ... }
*/
```

---

## 3748-头插

### 问题描述

本题要求你实现**不带头结点单链表**的头插操作。

头插定义：

* 在链表开头插入新节点
* 新节点成为第一个数据节点
* 必须修改头指针

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 头插 —— **本题核心**

```c
Status PushFront(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表开头插入元素 x
* 成功返回 `OK`

**失败条件**：

* `pL == NULL`：返回 `ERROR`
* 内存分配失败：返回 `ERROR`

**失败时**：不得修改链表

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **插入步骤**：
   * 分配新节点
   * 设置 `newNode->data = x`
   * 设置 `newNode->next = *pL`（指向原首节点）
   * 设置 `*pL = newNode`（修改头指针）

3. **内存安全**：分配失败时返回 ERROR。

4. **时间复杂度**：O(1)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 空表插入
  * 非空表插入

---

### 【提交说明】

* **只需提交 1 个函数**：`PushFront`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
Status PushFront(LinkList* pL, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 PushFront 时，学生只提交：
Status PushFront(LinkList* pL, ElementType x) { ... }
*/
```

---

## 3749-判空

### 问题描述

本题要求你实现**不带头结点单链表**的判空操作。

判空规则：

* 空表：头指针为 `NULL`
* 非空表：头指针指向第一个数据节点

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 判空 —— **本题核心**

```c
int IsEmpty(LinkList L);
```

**功能要求**：

* 判断链表 L 是否为空
* 空表返回 `1`
* 非空表返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。
2. **判空条件**：`L == NULL`。
3. **与带头结点的差异**：带头结点判空是 `L->next == NULL`，不带头结点判空是 `L == NULL`。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 测试会覆盖：
  
  * 空表
  * 非空表

---

### 【提交说明】

* **只需提交 1 个函数**：`IsEmpty`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
int IsEmpty(LinkList L);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 IsEmpty 时，学生只提交：
int IsEmpty(LinkList L) { ... }
*/
```

---

## 3750-获取长度

### 问题描述

本题要求你实现**不带头结点单链表**的获取长度操作。

长度定义：

* 长度 = 数据节点的个数
* 不带头结点链表没有头结点，直接从第一个数据节点开始计数

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 获取长度 —— **本题核心**

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表的长度（数据节点个数）
* 空表返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。
2. **遍历计数**：从 `L` 开始遍历，直到 `NULL`。
3. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 测试会覆盖：
  
  * 空表
  * 单元素
  * 多元素

---

### 【提交说明】

* **只需提交 1 个函数**：`GetLength`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
int GetLength(LinkList L);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 GetLength 时，学生只提交：
int GetLength(LinkList L) { ... }
*/
```

---

## 3751-按位置取值

### 问题描述

本题要求你实现**不带头结点单链表**的按位置取值操作。

位置定义：

* **1-based**：第1个数据节点位置为1，第2个为2，依此类推
* 不带头结点链表的第1个节点就是头指针指向的节点

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 按位置取值 —— **本题核心**

```c
Status GetElem(LinkList L, int pos1, ElementType* out);
```

**功能要求**：

* 获取位置 `pos1`（1-based）的元素值，存入 `*out`
* 成功返回 `OK`

**失败条件**：

* `L == NULL` 或 `out == NULL`：返回 `ERROR`
* `pos1 < 1`：返回 `ERROR`
* `pos1` 超出范围（大于链表长度）：返回 `ERROR`

**失败时**：不得修改 `*out`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。
2. **1-based 索引**：第1个数据节点的位置为1。
3. **遍历查找**：从 `L` 开始，遍历 `pos1-1` 次。
4. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 合法位置
  * pos1 < 1
  * pos1 > length
  * NULL 指针

---

### 【提交说明】

* **只需提交 1 个函数**：`GetElem`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
Status GetElem(LinkList L, int pos1, ElementType* out);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 GetElem 时，学生只提交：
Status GetElem(LinkList L, int pos1, ElementType* out) { ... }
*/
```

---

## 3752-按值查找

### 问题描述

本题要求你实现**不带头结点单链表**的按值查找操作。

查找规则：

* 返回第一个值等于 x 的数据节点的位置（**1-based**）
* 若不存在，返回 `-1`

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 按值查找 —— **本题核心**

```c
int LocateElem(LinkList L, ElementType x);
```

**功能要求**：

* 查找第一个值等于 x 的数据节点
* 返回其位置（1-based，第1个数据节点位置为1）
* 若不存在，返回 `-1`

**特殊情况**：

* `L == NULL`：返回 `-1`
* 空表：返回 `-1`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。
2. **1-based 索引**：第1个数据节点的位置为1。
3. **顺序查找**：从 `L` 开始遍历，直到找到或到达表尾。
4. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 找到元素
  * 未找到元素
  * 空表
  * NULL 指针

---

### 【提交说明】

* **只需提交 1 个函数**：`LocateElem`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
int LocateElem(LinkList L, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 LocateElem 时，学生只提交：
int LocateElem(LinkList L, ElementType x) { ... }
*/
```

---

## 3753-指定位置插入

### 问题描述

本题要求你实现**不带头结点单链表**的指定位置插入操作。

插入规则：

* 在位置 `pos1`（**1-based**）插入新节点
* 插入后，新节点占据位置 `pos1`，原 `pos1` 及之后的节点后移
* 当 `pos1 = 1` 时为头插，需要特殊处理

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 指定位置插入 —— **本题核心**

```c
Status InsertAt(LinkList* pL, int pos1, ElementType x);
```

**功能要求**：

* 在位置 `pos1`（1-based）插入元素 x
* 成功返回 `OK`

**失败条件**：

* `pL == NULL`：返回 `ERROR`
* `pos1 < 1`：返回 `ERROR`
* `pos1 > length + 1`（超出合法插入范围）：返回 `ERROR`
* 内存分配失败：返回 `ERROR`

**失败时**：不得修改链表

**特殊情况**：

* `pos1 = 1`：头插，需要修改头指针
* `pos1 = length + 1`：等价于尾插

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **1-based 索引**：第1个数据节点的位置为1。

3. **插入步骤**：
   * 若 `pos1 = 1`（头插）：
     * 设置 `newNode->next = *pL`
     * 设置 `*pL = newNode`
   * 否则：
     * 找到位置 `pos1-1` 的节点（前驱节点）
     * 设置 `newNode->next = p->next`
     * 设置 `p->next = newNode`

4. **内存安全**：分配失败时返回 ERROR。

5. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 头部插入（pos1=1）
  * 中间插入
  * 尾部插入（pos1=length+1）
  * 位置非法

---

### 【提交说明】

* **只需提交 1 个函数**：`InsertAt`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
Status InsertAt(LinkList* pL, int pos1, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 InsertAt 时，学生只提交：
Status InsertAt(LinkList* pL, int pos1, ElementType x) { ... }
*/
```

---

## 3754-删除指定位置

### 问题描述

本题要求你实现**不带头结点单链表**的删除指定位置操作。

删除规则：

* 删除位置 `pos1`（**1-based**）的数据节点
* 将被删除的元素值通过指针参数返回
* 删除后，后续节点前移
* 当 `pos1 = 1` 时为删除首节点，需要特殊处理

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 删除指定位置 —— **本题核心**

```c
Status DeleteAt(LinkList* pL, int pos1, ElementType* deleted);
```

**功能要求**：

* 删除位置 `pos1`（1-based）的数据节点
* 将被删除节点的值存入 `*deleted`
* 成功返回 `OK`

**失败条件**：

* `pL == NULL` 或 `*pL == NULL` 或 `deleted == NULL`：返回 `ERROR`
* `pos1 < 1`：返回 `ERROR`
* `pos1 > length`（超出范围）：返回 `ERROR`

**失败时**：不得修改链表和 `*deleted`

**特殊情况**：

* `pos1 = 1`：删除首节点，需要修改头指针

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **1-based 索引**：第1个数据节点的位置为1。

3. **删除步骤**：
   * 若 `pos1 = 1`（删除首节点）：
     * 保存待删除节点：`del = *pL`
     * 保存值：`*deleted = del->data`
     * 修改头指针：`*pL = del->next`
     * 释放内存：`free(del)`
   * 否则：
     * 找到位置 `pos1-1` 的节点（前驱节点）
     * 保存待删除节点：`del = p->next`
     * 保存值：`*deleted = del->data`
     * 修改链接：`p->next = del->next`
     * 释放内存：`free(del)`

4. **内存安全**：必须释放被删除节点。

5. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 删除头部（pos1=1）
  * 删除中间
  * 删除尾部（pos1=length）
  * 位置非法

---

### 【提交说明】

* **只需提交 1 个函数**：`DeleteAt`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
Status DeleteAt(LinkList* pL, int pos1, ElementType* deleted);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 DeleteAt 时，学生只提交：
Status DeleteAt(LinkList* pL, int pos1, ElementType* deleted) { ... }
*/
```

---

## 3755-删除所有指定值

### 问题描述

本题要求你实现**不带头结点单链表**的删除所有指定值操作。

删除规则：

* 删除链表中所有值等于 x 的数据节点
* 返回被删除节点的个数
* 需要先删除所有头部的 x，再删除中间的 x

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 删除所有指定值 —— **本题核心**

```c
int RemoveAll(LinkList* pL, ElementType x);
```

**功能要求**：

* 删除链表中所有值等于 x 的节点
* 返回被删除节点的个数

**特殊情况**：

* `pL == NULL`：返回 `0`
* 空表或无匹配值：返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **删除步骤**（推荐）：
   * **先删除所有头部的 x**（循环）：
     * 若 `*pL != NULL && (*pL)->data == x`
     * 保存 `del = *pL`
     * 修改 `*pL = del->next`
     * 释放 `free(del)`
   * **再删除中间的 x**（使用前驱指针法）：
     * 使用指针 p 指向当前节点（从 `*pL` 开始）
     * 若 `p->next->data == x`：删除 `p->next`，p 不移动
     * 否则：p 向后移动

3. **内存安全**：必须释放所有被删除节点。

4. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 无匹配值
  * 部分匹配
  * 全部匹配
  * 连续匹配值

---

### 【提交说明】

* **只需提交 1 个函数**：`RemoveAll`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
int RemoveAll(LinkList* pL, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 RemoveAll 时，学生只提交：
int RemoveAll(LinkList* pL, ElementType x) { ... }
*/
```

---

## 3756-逆置

### 问题描述

本题要求你实现**不带头结点单链表**的逆置操作。

逆置定义：

* 将链表中数据节点的顺序反转
* 需要修改头指针，使其指向原来的尾节点

例如：`1 -> 2 -> 3 -> 4` 逆置后变为 `4 -> 3 -> 2 -> 1`

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 逆置 —— **本题核心**

```c
void Reverse(LinkList* pL);
```

**功能要求**：

* 原地逆置链表中的数据节点
* 修改头指针，使其指向原尾节点

**特殊情况**：

* `pL == NULL` 或 `*pL == NULL`（空表）：直接返回，不做任何操作
* 单节点：直接返回

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **原地逆置**：不允许分配新节点，只改变 `next` 指针。

3. **推荐算法（三指针法）**：
   * 使用三个指针：`prev`（初始为 NULL）、`curr`（初始为 `*pL`）、`nextNode`
   * 遍历链表，依次将每个节点的 `next` 指向前驱
   * 最后将 `*pL` 指向新的首节点（原尾节点）

4. **时间复杂度**：O(n)。

5. **空间复杂度**：O(1)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 元素值为 `int`，范围：-2³¹ \~ 2³¹-1
* 测试会覆盖：
  
  * 空表
  * 单节点
  * 多节点

---

### 【提交说明】

* **只需提交 1 个函数**：`Reverse`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
void Reverse(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 Reverse 时，学生只提交：
void Reverse(LinkList* pL) { ... }
*/
```

---

## 3757-清空

### 问题描述

本题要求你实现**不带头结点单链表**的清空操作。

清空定义：

* 删除所有数据节点
* 将头指针置为 `NULL`
* 清空后链表变为空表

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 清空 —— **本题核心**

```c
void Clear(LinkList* pL);
```

**功能要求**：

* 删除所有数据节点
* 将头指针置为 `NULL`
* 清空后 `*pL = NULL`

**特殊情况**：

* `pL == NULL`：直接返回，不做任何操作
* 空表：直接返回

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

void Clear(LinkList* pL) {
	if (pL == NULL) return;
	
	LNode* p = *pL;
	while (p) {
		LNode* del = p;
		p = p->next;
		free(del);
	}
	*pL = NULL;
}

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
void Clear(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 Clear 时，学生只提交：
void Clear(LinkList* pL) { ... }
*/
```

---

## 3758-销毁

### 问题描述

本题要求你实现**不带头结点单链表**的销毁操作。

销毁定义：

* 删除所有数据节点
* 将头指针置为 `NULL`
* 对于不带头结点链表，销毁操作等同于清空操作

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **1 个函数**：

### 销毁 —— **本题核心**

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 删除所有数据节点
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

**特殊情况**：

* `pL == NULL` 或 `*pL == NULL`：直接返回，不做任何操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的链表结构**完成任务。

2. **销毁步骤**：
   * 对于不带头结点链表，销毁操作等同于清空操作
   * 可以直接调用 `Clear(*pL)` 或手动实现
   * 逐个释放所有数据节点
   * 置空指针：`*pL = NULL`

3. **内存安全**：必须释放所有节点。

4. **重复销毁安全**：`pL == NULL` 或 `*pL == NULL` 时直接返回。

5. **时间复杂度**：O(n)。

---

### 【数据范围】

* 链表节点个数：0 \~ 1000
* 测试会覆盖：
  
  * 正常销毁
  * 重复销毁
  * NULL 指针

---

### 【提交说明】

* **只需提交 1 个函数**：`DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数 */
void DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如缺失 DestroyList 时，学生只提交：
void DestroyList(LinkList* pL) { ... }
*/
```

---

## 3759-逆置区间

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**逆置指定区间**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个单链表和两个位置参数 start 和 end（从1开始计数），将区间 \[start, end] 内的节点就地逆置：

* 只逆置指定区间内的节点，区间外的节点保持不变

* 不允许开辟新节点，必须在原链表上修改指针完成逆置

* 若 start == end，链表保持不变

* 若 start > end，返回 ERROR

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）逆置指定区间

```c
Status ReverseSegment(LinkList* pL, int start, int end);
```

**功能要求**：

* 将链表中位置 \[start, end] 区间内的节点就地逆置（位置从1开始）

* 不允许开辟新节点，必须修改原链表指针

* 区间外的节点保持原有顺序和连接关系

* 若 start == end，链表保持不变，返回 `OK`

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* `start < 1` 或 `end < 1`

* `start > end`

* `start` 或 `end` 超出链表长度

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **就地逆置是强约束**：不允许开辟新节点，必须通过修改指针完成。
3. **区间边界处理**：需要正确处理区间起点前一个节点和区间终点后一个节点的连接。
4. **特殊情况处理**：区间包含首节点时需要更新 `*pL`。
5. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
6. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：1 ≤ n ≤ 100
* 区间位置：1 ≤ start ≤ end ≤ n
* 操作次数：1 ≤ m ≤ 20
* 测试会覆盖：

  * 逆置整个链表（start=1, end=n）
  * 逆置首部区间（包含首节点）
  * 逆置中间区间
  * 逆置尾部区间
  * 单节点区间（start == end）
  * 非法参数验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `ReverseSegment` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status ReverseSegment(LinkList* pL, int start, int end);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status ReverseSegment(LinkList* pL, int start, int end) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3760-删除最小值

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**删除最小值节点**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个单链表，删除链表中值最小的节点：

* 若存在多个最小值节点，只删除第一个

* 删除后通过指针参数返回被删除节点的值

* 删除节点后需要正确释放内存

若参数非法或链表为空，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）删除最小值节点

```c
Status RemoveMin(LinkList* pL, ElementType* minVal);
```

**功能要求**：

* 删除链表中值最小的节点

* 若存在多个最小值，只删除第一个出现的

* 通过 `*minVal` 返回被删除节点的值

* 删除节点后正确释放内存

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL` 或 `minVal == NULL`

* `*pL == NULL`（链表为空）

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **查找最小值需要遍历整个链表**：一次遍历找到最小值及其前驱节点。
3. **首节点删除特殊处理**：若最小值在首节点，需要更新 `*pL`。
4. **内存释放**：删除节点后必须释放其内存。
5. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
6. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：1 ≤ n ≤ 100
* 删除操作次数：1 ≤ m ≤ n
* 测试会覆盖：

  * 最小值在首节点
  * 最小值在中间节点
  * 最小值在尾节点
  * 多个最小值（只删第一个）
  * 删除至空表
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `RemoveMin` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status RemoveMin(LinkList* pL, ElementType* minVal);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status RemoveMin(LinkList* pL, ElementType* minVal) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3761-拆分链表

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**按值拆分链表**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个单链表和分界值 x，将链表拆分为两个链表：

* 第一个链表包含所有值小于 x 的节点（保持原有顺序）

* 第二个链表包含所有值大于等于 x 的节点（保持原有顺序）

* 原链表的节点被重新分配到两个新链表中，不开辟新节点

* 拆分后原链表指针置为 NULL

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）按值拆分链表

```c
Status SplitByValue(LinkList* pL, ElementType x, LinkList* pLess, LinkList* pGreaterEqual);
```

**功能要求**：

* 将链表拆分为两个链表：

  * `*pLess`：所有值 < x 的节点（保持原有顺序）

  * `*pGreaterEqual`：所有值 ≥ x 的节点（保持原有顺序）

* 不允许开辟新节点，必须重用原链表节点

* 拆分后将 `*pL` 置为 `NULL`

* 若原链表所有节点都属于某一类，另一类链表为 `NULL`

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL` 或 `pLess == NULL` 或 `pGreaterEqual == NULL`

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **节点重用是强约束**：不允许开辟新节点，必须重用原链表节点。
3. **保持原有顺序**：拆分后两个链表中的节点顺序与原链表中的相对顺序一致。
4. **原链表置空**：拆分完成后必须将 `*pL` 置为 `NULL`。
5. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
6. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：1 ≤ n ≤ 100
* 分界值 x：-100 ≤ x ≤ 100
* 操作次数：1 ≤ m ≤ 10
* 测试会覆盖：

  * 所有节点 < x
  * 所有节点 ≥ x
  * 部分节点 < x，部分 ≥ x
  * 单节点链表
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `SplitByValue` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status SplitByValue(LinkList* pL, ElementType x, LinkList* pLess, LinkList* pGreaterEqual);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status SplitByValue(LinkList* pL, ElementType x, LinkList* pLess, LinkList* pGreaterEqual) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3762-删除区间值

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**删除区间值节点**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个单链表和值区间 \[low, high]，删除链表中所有值在该区间内的节点：

* 删除所有满足 low ≤ data ≤ high 的节点

* 删除后需要正确释放内存

* 保持剩余节点的原有顺序

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）删除区间值节点

```c
Status RemoveRange(LinkList* pL, ElementType low, ElementType high);
```

**功能要求**：

* 删除链表中所有满足 low ≤ data ≤ high 的节点

* 删除节点后正确释放内存

* 若区间内无节点，链表保持不变，返回 `OK`

* 若删除后链表为空，将 `*pL` 置为 `NULL`

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* `low > high`

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **区间判断**：节点值必须同时满足 ≥ low 且 ≤ high 才删除。
3. **首节点处理**：删除首节点时需要更新 `*pL`。
4. **内存释放**：删除节点后必须释放其内存。
5. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
6. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：1 ≤ n ≤ 100
* 区间范围：-100 ≤ low ≤ high ≤ 100
* 操作次数：1 ≤ m ≤ 10
* 测试会覆盖：

  * 删除所有节点
  * 删除部分节点
  * 区间内无节点
  * 连续删除多个节点
  * 删除首节点/尾节点/中间节点
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `RemoveRange` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status RemoveRange(LinkList* pL, ElementType low, ElementType high);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status RemoveRange(LinkList* pL, ElementType low, ElementType high) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3763-有序合并

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**合并两个有序链表**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定两个非递减有序链表 La 和 Lb，将它们合并为一个新的非递减有序链表 Lc：

* 合并后的链表 Lc 包含 La 和 Lb 的所有节点

* Lc 保持非递减有序

* 不允许开辟新节点，必须重用 La 和 Lb 的节点

* 合并后 La 和 Lb 的指针均置为 NULL

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）合并两个有序链表

```c
Status MergeSorted(LinkList* pLa, LinkList* pLb, LinkList* pLc);
```

**功能要求**：

* 将两个非递减有序链表 La 和 Lb 合并为一个新的非递减有序链表 Lc

* 不允许开辟新节点，必须重用 La 和 Lb 的节点

* 合并后将 `*pLa` 和 `*pLb` 置为 `NULL`

* 若 La 或 Lb 为空，直接将非空链表赋给 Lc

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pLa == NULL` 或 `pLb == NULL` 或 `pLc == NULL`

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **节点重用是强约束**：不允许开辟新节点，必须重用 La 和 Lb 的节点。
3. **有序性维护**：合并后链表必须保持非递减有序。
4. **原链表置空**：合并完成后必须将 `*pLa` 和 `*pLb` 置为 `NULL`。
5. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
6. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：0 ≤ n, m ≤ 50
* 合并操作次数：1 ≤ k ≤ 10
* 测试会覆盖：

  * 两个空表合并
  * 一个空表与非空表合并
  * 两个非空有序表合并
  * 元素完全无交叉
  * 元素有交叉重叠
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `MergeSorted` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status MergeSorted(LinkList* pLa, LinkList* pLb, LinkList* pLc);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status MergeSorted(LinkList* pLa, LinkList* pLb, LinkList* pLc) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3764-去重

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**删除有序链表中的重复节点**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个非递减有序链表，删除链表中所有重复的节点，使每个元素只保留一个：

* 删除后的链表仍保持非递减有序

* 原地修改链表，不允许开辟新节点

* 每个值只保留第一次出现的节点

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）删除有序链表中的重复节点

```c
Status Unique(LinkList* pL);
```

**功能要求**：

* 删除链表中所有重复的节点，使每个元素只保留一个

* 原地修改链表，不允许开辟新节点

* 每个值只保留第一次出现的节点

* 删除后的链表仍保持非递减有序

* 若链表为空或只有一个节点，无需操作

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **原地修改是强约束**：不允许开辟新节点，必须在原链表上删除重复节点。
3. **有序性维护**：删除后链表必须保持非递减有序。
4. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
5. **内存安全**：所有被删除的节点必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：0 ≤ n ≤ 100
* 去重操作次数：1 ≤ m ≤ 10
* 测试会覆盖：

  * 空链表
  * 无重复元素
  * 全部元素相同
  * 部分元素重复
  * 单节点链表
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `Unique` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status Unique(LinkList* pL);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status Unique(LinkList* pL) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---

## 3765-集合交集

### 问题描述

本题要求你实现**不带头结点单链表**的基本操作，并在此基础上完成**求两个集合的交集**操作。

---

## 链表类型定义

```c
typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定两个非递减有序链表 La 和 Lb（分别代表两个集合），求它们的交集并存储到新链表 Lc 中：

* 交集中包含同时出现在 La 和 Lb 中的元素

* Lc 保持非递减有序且无重复元素

* 不允许开辟新节点，必须从 La 中选取节点构成 Lc

* 操作后 La 和 Lb 的指针均置为 NULL

若参数非法，操作失败：必须返回 `ERROR` 且不得修改链表。

---

## 函数说明

你需要实现以下 **7 个函数**：

### 1）创建并初始化链表

```c
Status InitList(LinkList* pL);
```

**功能要求**：

* 将 `*pL` 初始化为 `NULL`，表示空链表

* 若 `pL == NULL`，返回 `ERROR`

* 否则返回 `OK`

---

### 2）判断链表是否为空

```c
Status IsEmpty(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，返回 `OK`

* 否则返回 `ERROR`

---

### 3）获取链表长度

```c
int GetLength(LinkList L);
```

**功能要求**：

* 返回链表中节点的个数

* 若 `L == NULL`，返回 `0`

---

### 4）尾插法插入元素

```c
Status PushBack(LinkList* pL, ElementType x);
```

**功能要求**：

* 在链表尾部插入元素 x

* 若链表为空，插入后该节点成为首节点

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pL == NULL`

* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 5）输出链表

```c
void PrintList(LinkList L);
```

**功能要求**：

* 若 `L == NULL`，输出 `NULL` 并换行

* 否则按格式输出：

  * 输出所有元素，元素间用空格分隔，最后换行

---

### 6）求两个集合的交集

```c
Status Intersection(LinkList* pLa, LinkList* pLb, LinkList* pLc);
```

**功能要求**：

* 求两个非递减有序链表 La 和 Lb 的交集，结果存储到 Lc 中

* 交集中包含同时出现在 La 和 Lb 中的元素

* Lc 保持非递减有序且无重复元素

* 不允许开辟新节点，必须从 La 中选取节点构成 Lc

* 操作后将 `*pLa` 和 `*pLb` 置为 `NULL`（释放所有节点）

* 若交集为空，`*pLc` 置为 `NULL`

* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `pLa == NULL` 或 `pLb == NULL` 或 `pLc == NULL`

**失败时**：必须返回 `ERROR` 且**不得修改链表**

---

### 7）销毁链表

```c
void DestroyList(LinkList* pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回

* 释放所有节点

* 将 `*pL` 置为 `NULL`

* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

\##【实现要求】

1. **必须使用本题给定的不带头结点单链表结构与接口**完成任务，不允许使用 STL 容器替代链表存储。
2. **节点重用是强约束**：不允许开辟新节点，必须从 La 中选取节点构成 Lc。
3. **有序性维护**：交集链表必须保持非递减有序。
4. **去重要求**：交集链表中不得包含重复元素。
5. **原链表销毁**：操作完成后必须将 `*pLa` 和 `*pLb` 置为 `NULL`，释放所有节点。
6. **参数非法不修改链表**：任何失败条件下不得改变链表结构。
7. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

\##【数据范围】

* 元素值为 `int`，可能包含负数
* 链表长度：0 ≤ n, m ≤ 50
* 交集操作次数：1 ≤ k ≤ 10
* 测试会覆盖：

  * 两个空表
  * 一个空表与非空表
  * 无交集
  * 完全相同
  * 部分交集
  * 单节点链表
  * NULL 安全性验证

\##【提交说明】

* **只需提交 7 个函数**：`InitList` / `IsEmpty` / `GetLength` / `PushBack` / `PrintList` / `Intersection` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;

typedef struct LNode {
    ElementType data;
    struct LNode* next;
} LNode, *LinkList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 7 个函数 */
Status InitList(LinkList* pL);
Status IsEmpty(LinkList L);
int    GetLength(LinkList L);
Status PushBack(LinkList* pL, ElementType x);
void   PrintList(LinkList L);
Status Intersection(LinkList* pLa, LinkList* pLb, LinkList* pLc);
void   DestroyList(LinkList* pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 7 个函数的实现。
     */
    return 0;
}

/******************** 学生提交的&quot;缺失函数&quot;会被拼接在此处 ********************/
/*
例如你需要提交以下 7 个函数的完整实现：

Status InitList(LinkList* pL) {
    // 你的实现代码
}

Status IsEmpty(LinkList L) {
    // 你的实现代码
}

int GetLength(LinkList L) {
    // 你的实现代码
}

Status PushBack(LinkList* pL, ElementType x) {
    // 你的实现代码
}

void PrintList(LinkList L) {
    // 你的实现代码
}

Status Intersection(LinkList* pLa, LinkList* pLb, LinkList* pLc) {
    // 你的实现代码
}

void DestroyList(LinkList* pL) {
    // 你的实现代码
}
*/
```

---
