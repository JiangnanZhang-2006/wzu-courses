# 作业01 动态扩容顺序表

> 来源：[课程题集](https://10-132-246-246.webvpn.wzu.edu.cn/course/7/volume/726/detail/)

## 目录

1. [3703-创建与初始化](#3703-创建与初始化)
2. [3704-表尾插入](#3704-表尾插入)
3. [3705-输出顺序表](#3705-输出顺序表)
4. [3706-判空](#3706-判空)
5. [3707-判满](#3707-判满)
6. [3708-获取长度](#3708-获取长度)
7. [3709-按位置取值](#3709-按位置取值)
8. [3710-按值查找](#3710-按值查找)
9. [3711-指定位置插入](#3711-指定位置插入)
10. [3712-删除指定位置](#3712-删除指定位置)
11. [3713-删除所有指定值](#3713-删除所有指定值)
12. [3714-逆置顺序表](#3714-逆置顺序表)
13. [3715-清空顺序表](#3715-清空顺序表)
14. [3716-销毁顺序表](#3716-销毁顺序表)
15. [3717-扩容容量](#3717-扩容容量)
16. [3718-批量插入](#3718-批量插入)
17. [3719-多表合并](#3719-多表合并)
18. [3720-弹性删除区间](#3720-弹性删除区间)
19. [3721-去重压缩](#3721-去重压缩)
20. [3722-区间统计与修改](#3722-区间统计与修改)
21. [3723-循环左移](#3723-循环左移)

---

## 3703-创建与初始化

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现创建与初始化操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态分配的数组
    int length;         // 当前长度（已存元素个数）
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `SeqList* InitList(void)` 函数，实现创建并初始化一个空的动态扩容顺序表。

**说明**：本题测试会进行"创建→插入→输出"等实际使用验证，而不是只检查指针是否为空。

## 函数说明

**函数原型**：

```c
SeqList* InitList(void);
```

**参数说明**：

* 无

**返回值说明**：

* 创建成功：返回指向新顺序表的指针
* 创建失败（内存申请失败）：返回 NULL

**特殊情况处理**：

* 创建后必须满足：

  * `length == 0`
  * `capacity == 10`（初始容量）
  * `data` 指针已成功分配内存
* 若返回 NULL，测试代码会按约定处理（你无需输出任何提示信息）。

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 顺序表初始容量：`capacity = 10`
* 初始化后元素个数：`length = 0`
* 元素类型：`ElementType = int`
* （本题仅初始化，不涉及元素值范围）

**【提交说明】**

* 本题有预置代码，只需提交 `InitList` 函数的实现。
* 不要提交 main 函数或其它辅助函数。
* 不要重复提交预置代码中已经给出的结构体定义与宏。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
SeqList* InitList(void);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3704-表尾插入

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现表尾插入操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `Status PushBack(SeqList* L, ElementType x)` 函数，实现在顺序表尾部插入元素。

**核心差异**：当容量不足时，函数应自动调用 `ExpandCapacity(L)` 进行扩容，而不是返回失败。

### 函数说明

**函数原型**：

```c
Status PushBack(SeqList* L, ElementType x);
```

**参数说明**：

* `L`：指向顺序表的指针
* `x`：待插入的元素值

**返回值说明**：

* 插入成功：返回 `OK`
* 插入失败（L为NULL或扩容失败）：返回 `ERROR`

**特殊情况处理**：

* 若 `L == NULL`，返回 `ERROR`
* 若容量已满（`length >= capacity`），先调用 `ExpandCapacity(L)` 扩容
* 若扩容失败，返回 `ERROR`
* 扩容成功后继续插入操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 元素值范围：int类型全范围
* 测试将包含大量插入（可能触发多次扩容）

**【提交说明】**

* 本题有预置代码，只需提交 `PushBack` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 可以调用已提供的 `ExpandCapacity` 函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
Status PushBack(SeqList* L, ElementType x);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}
/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3705-输出顺序表

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现输出操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `void PrintList(const SeqList* L)` 函数，实现按顺序输出表中所有元素。

### 函数说明

**函数原型**：

```c
void PrintList(const SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）

**返回值说明**：

* 无返回值

**输出格式**：

* 若表为空（L为NULL或length为0）：输出空行
* 若表非空：元素之间用单个空格分隔，末尾换行
* 示例：`1 2 3 4 5\n`

**特殊情况处理**：

* 若 `L == NULL`，输出空行
* 若 `length == 0`，输出空行

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 表长范围：0 到实际容量上限
* 输出格式严格要求：元素间单空格，无多余空格

**【提交说明】**

* 本题有预置代码，只需提交 `PrintList` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
void PrintList(const SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3706-判空

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现判空操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `int IsEmpty(const SeqList* L)` 函数，判断顺序表是否为空。

### 函数说明

**函数原型**：

```c
int IsEmpty(const SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）

**返回值说明**：

* 表为空（L为NULL或length为0）：返回 `1`
* 表非空：返回 `0`

**特殊情况处理**：

* 若 `L == NULL`，视为空表，返回 `1`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 返回值：0 或 1

**【提交说明】**

* 本题有预置代码，只需提交 `IsEmpty` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
int IsEmpty(const SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3707-判满

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现判满操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `int IsFull(const SeqList* L)` 函数，判断顺序表是否已满。

**核心差异**：动态扩容顺序表理论上永不会满（可自动扩容），因此本函数应始终返回 `0`。

### 函数说明

**函数原型**：

```c
int IsFull(const SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）

**返回值说明**：

* 动态扩容版本始终返回 `0`（表示未满）
* 若 `L == NULL`，返回 `0`
**特殊情况处理**：

* 与固定容量版本不同，动态扩容版本无需检查 `length >= capacity`
* 即使当前 `length == capacity`，下次插入会自动扩容

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 返回值：始终为 `0`
* 本题体现动态扩容与固定容量的核心差异

**【提交说明】**

* 本题有预置代码，只需提交 `IsFull` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
int IsFull(const SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3708-获取长度

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现获取长度操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `int GetLength(const SeqList* L)` 函数，返回顺序表的当前长度（已存元素个数）。

### 函数说明

**函数原型**：

```c
int GetLength(const SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）

**返回值说明**：

* 返回表的当前长度（`length` 字段值）
* 若 `L == NULL`，返回 `0`

**特殊情况处理**：

* 若 `L == NULL`，返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 长度范围：0 到实际容量上限

**【提交说明】**

* 本题有预置代码，只需提交 `GetLength` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
int GetLength(const SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3709-按位置取值

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现按位置取值操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `Status GetElem(const SeqList* L, int pos1, ElementType* out)` 函数，获取指定位置的元素值。

**位置约定**：使用 1-based 索引（第1个元素位置为1）。

### 函数说明

**函数原型**：

```c
Status GetElem(const SeqList* L, int pos1, ElementType* out);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）
* `pos1`：目标位置（1-based，范围应为 1 到 length）
* `out`：输出参数，用于存储获取的元素值

**返回值说明**：

* 获取成功：返回 `OK`，并通过 `out` 参数返回元素值
* 获取失败（L为NULL、out为NULL或位置非法）：返回 `ERROR`

**特殊情况处理**：

* 若 `L == NULL` 或 `out == NULL`，返回 `ERROR`
* 若 `pos1 < 1` 或 `pos1 > length`，返回 `ERROR`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 位置范围：合法位置为 1 到 length
* 测试将包含边界位置和非法位置

**【提交说明】**

* 本题有预置代码，只需提交 `GetElem` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
Status GetElem(const SeqList* L, int pos1, ElementType* out);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3710-按值查找

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现按值查找操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `int LocateElem(const SeqList* L, ElementType x)` 函数，查找指定值在表中的位置。

**位置约定**：使用 1-based 索引（第1个元素位置为1）。

### 函数说明

**函数原型**：

```c
int LocateElem(const SeqList* L, ElementType x);
```

**参数说明**：

* `L`：指向顺序表的指针（const修饰，表示只读）
* `x`：待查找的元素值

**返回值说明**：

* 找到：返回第一次出现的位置（1-based）
* 未找到或L为NULL：返回 `-1`

**特殊情况处理**：

* 若 `L == NULL`，返回 `-1`
* 若表中有多个相同值，返回第一次出现的位置

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 元素值范围：int类型全范围
* 测试将包含存在、不存在、重复值等情况

**【提交说明】**

* 本题有预置代码，只需提交 `LocateElem` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
int LocateElem(const SeqList* L, ElementType x);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3711-指定位置插入

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现指定位置插入操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `Status InsertAt(SeqList* L, int pos1, ElementType x)` 函数，在指定位置插入元素。

**位置约定**：使用 1-based 索引（第1个元素位置为1）。

**核心差异**：当容量不足时，函数应自动调用 `ExpandCapacity(L)` 进行扩容，而不是返回失败。

### 函数说明

**函数原型**：

```c
Status InsertAt(SeqList* L, int pos1, ElementType x);
```

**参数说明**：

* `L`：指向顺序表的指针
* `pos1`：插入位置（1-based，范围应为 1 到 length+1）
* `x`：待插入的元素值

**返回值说明**：

* 插入成功：返回 `OK`
* 插入失败（L为NULL、位置非法或扩容失败）：返回 `ERROR`

**特殊情况处理**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `pos1 < 1` 或 `pos1 > length + 1`，返回 `ERROR`
* 若容量已满，先调用 `ExpandCapacity(L)` 扩容
* 若扩容失败，返回 `ERROR`
* 插入后，pos1及之后的元素依次后移

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 元素值范围：int类型全范围
* 位置范围：合法位置为 1 到 length+1
* 测试将包含头插、尾插、中间插入、非法位置、多次扩容等情况

**【提交说明】**

* 本题有预置代码，只需提交 `InsertAt` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 可以调用已提供的 `ExpandCapacity` 函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
Status InsertAt(SeqList* L, int pos1, ElementType x);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3712-删除指定位置

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现删除指定位置操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `Status DeleteAt(SeqList* L, int pos1, ElementType* deleted)` 函数，删除指定位置的元素。

**位置约定**：使用 1-based 索引（第1个元素位置为1）。

### 函数说明

**函数原型**：

```c
Status DeleteAt(SeqList* L, int pos1, ElementType* deleted);
```

**参数说明**：

* `L`：指向顺序表的指针
* `pos1`：删除位置（1-based，范围应为 1 到 length）
* `deleted`：输出参数，用于存储被删除的元素值

**返回值说明**：

* 删除成功：返回 `OK`，并通过 `deleted` 参数返回被删除的元素值
* 删除失败（L为NULL、deleted为NULL或位置非法）：返回 `ERROR`

**特殊情况处理**：

* 若 `L == NULL` 或 `deleted == NULL`，返回 `ERROR`
* 若 `pos1 < 1` 或 `pos1 > length`，返回 `ERROR`
* 删除后，pos1之后的元素依次前移

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 位置范围：合法位置为 1 到 length
* 测试将包含头删、尾删、中间删除、非法位置等情况

**【提交说明】**

* 本题有预置代码，只需提交 `DeleteAt` 函数的实现
* 不要提交 main 函数或其它辅助函数

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
Status DeleteAt(SeqList* L, int pos1, ElementType* deleted);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3713-删除所有指定值

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现删除所有指定值操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `int RemoveAll(SeqList* L, ElementType x)` 函数，删除顺序表中所有值等于 `x` 的元素，并返回删除的元素个数。

## 函数说明

**函数原型**：

```c
int RemoveAll(SeqList* L, ElementType x);
```

**参数说明**：

* `L`：指向顺序表的指针
* `x`：待删除的目标值

**返回值说明**：

* 返回实际删除的元素个数
* 若 `L == NULL`，返回 `0`
* 若表中不存在目标值，返回 `0`

**特殊情况处理**：

* 若 `L == NULL`，返回 `0`
* 删除操作应保持剩余元素的相对顺序
* 使用双指针法一次遍历完成（避免多次移动）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 元素值范围：int类型全范围
* 测试将包含：不存在、单次出现、多次出现、全部删除等情况

**【提交说明】**

* 本题有预置代码，只需提交 `RemoveAll` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 建议使用双指针法优化效率

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
int RemoveAll(SeqList* L, ElementType x);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3714-逆置顺序表

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现逆置操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `void Reverse(SeqList* L)` 函数，将顺序表中的元素顺序反转。

### 函数说明

**函数原型**：

```c
void Reverse(SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针

**返回值说明**：

* 无返回值

**操作效果**：

* 将表中元素顺序完全反转
* 例如：`[1, 2, 3, 4, 5]` → `[5, 4, 3, 2, 1]`

**特殊情况处理**：

* 若 `L == NULL`，无操作
* 若表为空或只有1个元素，无需操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 元素类型：`ElementType = int`
* 测试将包含空表、单元素、多元素、奇偶长度等情况
* 测试将验证可逆性（连续逆置两次应恢复原状）

**【提交说明】**

* 本题有预置代码，只需提交 `Reverse` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 建议使用双指针法原地反转

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
void Reverse(SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3715-清空顺序表

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现清空操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `void Clear(SeqList* L)` 函数，清空顺序表中的所有元素。

**注意**：清空操作只重置长度，不释放 `data` 指针指向的内存空间。

### 函数说明

**函数原型**：

```c
void Clear(SeqList* L);
```

**参数说明**：

* `L`：指向顺序表的指针

**返回值说明**：

* 无返回值

**操作效果**：

* 将 `length` 重置为 `0`
* 保持 `capacity` 和 `data` 指针不变
* 清空后应能继续插入新元素

**特殊情况处理**：

* 若 `L == NULL`，无操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 清空后：`length = 0`，`capacity` 保持不变
* 测试将验证清空后的判空状态和可继续插入性

**【提交说明】**

* 本题有预置代码，只需提交 `Clear` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 注意：不要释放 `data` 内存（与销毁操作区分）

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
void Clear(SeqList* L);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3716-销毁顺序表

### 问题描述

本题在"动态扩容顺序表（动态数组）"数据结构上，实现销毁操作。

给定动态扩容顺序表的类型定义如下：

```c
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

设计 `void DestroyList(SeqList** pL)` 函数，释放顺序表占用的所有内存资源。

**核心差异**：动态扩容版本需要释放两层内存（`data` 数组 + 结构体本身）。

### 函数说明

**函数原型**：

```c
void DestroyList(SeqList** pL);
```

**参数说明**：

* `pL`：指向顺序表指针的指针（二级指针）

**返回值说明**：

* 无返回值

**操作效果**：

* 释放 `(*pL)->data` 指向的内存
* 释放 `*pL` 指向的结构体内存
* 将 `*pL` 设置为 `NULL`

**特殊情况处理**：

* 若 `pL == NULL`，无操作
* 若 `*pL == NULL`，无操作
* 若 `(*pL)->data != NULL`，先释放 `data` 再释放结构体

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

**【数据范围】**

* 销毁后：`*pL == NULL`
* 测试将验证完整生命周期管理（防止内存泄漏）

**【提交说明】**

* 本题有预置代码，只需提交 `DestroyList` 函数的实现
* 不要提交 main 函数或其它辅助函数
* 注意：必须释放两层内存（`data` + 结构体）

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/******************** 函数声明（本题要求实现） ********************/
void DestroyList(SeqList** pL);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3717-扩容容量

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**扩容容量**操作。

---

## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个顺序表和目标容量，将顺序表的容量扩展到指定大小：

* 若目标容量 ≤ 当前容量，不做任何操作，返回 OK
* 若目标容量 > 当前容量，扩容到目标容量
* 扩容后保持原有数据不变

若参数非法或内存分配失败，操作失败：必须返回 `ERROR` 且不得修改顺序表。

---

## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）扩容容量 —— **本题核心**

```c
Status ExpandCapacity(SeqList* L, int newCapacity);
```

**功能要求**：

* 将顺序表 L 的容量扩展到 newCapacity
* 若 `newCapacity <= L->capacity`，不做任何操作，返回 `OK`
* 若 `newCapacity > L->capacity`，扩容到 newCapacity，保持原有数据不变
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* `newCapacity < 0`
* 内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改 L**（包括 `L->capacity`、`L->data` 和 `L->length`）

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. **失败不修改 L 是强约束**：参数非法或扩容失败时不得改变 `L->capacity`、`L->data` 和 `L->length`。
3. **扩容策略**：目标容量 ≤ 当前容量时，不做任何操作直接返回 OK。
4. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 扩容目标容量：0 ≤ newCapacity ≤ 10000
* 测试会覆盖：

  * 扩容成功（容量增大）
  * 不扩容（目标容量 ≤ 当前容量）
  * NULL 安全性验证
  * 失败不修改 L

【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `ExpandCapacity` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   ExpandCapacity(SeqList* L, int newCapacity);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3718-批量插入

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**批量插入**操作。

---

## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

给定一个顺序表、插入位置和一组元素，将这些元素批量插入到指定位置：

* 插入位置必须合法（0 ≤ pos ≤ length）
* 插入后保持原有元素相对顺序不变
* 若容量不足需自动扩容

若参数非法或内存分配失败，操作失败：必须返回 `ERROR` 且不得修改顺序表。

---

# 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）批量插入 —— **本题核心**

```c
Status InsertRange(SeqList* L, int pos, const ElementType* arr, int count);
```

**功能要求**：

* 在位置 `pos` 处插入 `count` 个元素（来自数组 `arr`）
* 插入位置必须合法：`0 <= pos <= L->length`
* 插入后，原 `pos` 位置及之后的元素后移 `count` 位
* 若容量不足，需扩容到能容纳所有元素（建议按 2 倍扩容）
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* `arr == NULL`（当 `count > 0` 时）
* `pos < 0` 或 `pos > L->length`
* `count < 0`
* 扩容失败（内存分配失败）

**失败时**：必须返回 `ERROR` 且**不得修改 L**（包括 `L->length`、`L->capacity` 和 `L->data` 的内容）

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `InsertRange` 需要处理元素后移，推荐使用 `memmove` 或循环实现。
3. **失败不修改 L 是强约束**：参数非法或扩容失败时不得改变 `L->length`、`L->capacity` 和 `L->data` 的内容。
4. **扩容策略**：容量不足时，建议按 2 倍扩容，直到能容纳所有元素。
5. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 插入位置：0 ≤ pos ≤ length
* 批量插入元素个数：0 ≤ count ≤ 10000
* 测试会覆盖：

  * 头部插入、中间插入、尾部插入
  * 插入 0 个元素
  * 需要多次扩容
  * 位置非法
  * NULL 安全性验证
  * 失败不修改 L

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `InsertRange` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   InsertRange(SeqList* L, int pos, const ElementType* arr, int count);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3719-多表合并

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**合并多个顺序表**操作。

给定多个顺序表，将它们依次合并到一个目标顺序表中：

* 合并顺序：按输入顺序依次将每个表的所有元素追加到目标表
* 合并后保持原有元素相对顺序不变
* 若容量不足需自动扩容

若参数非法或内存分配失败，操作失败：必须返回 `ERROR` 且不得修改目标顺序表。

---

## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）合并多个顺序表 —— **本题核心**

```c
Status MergeLists(SeqList* dest, SeqList** sources, int count);
```

**功能要求**：

* 将 `count` 个顺序表（存储在 `sources` 数组中）依次合并到 `dest`
* 合并前先清空 `dest`（`length = 0`）
* 按 `sources[0], sources[1], ..., sources[count-1]` 的顺序依次追加所有元素
* 若容量不足，需扩容到能容纳所有元素（建议按 2 倍扩容）
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `dest == NULL` 或 `dest->data == NULL`
* `sources == NULL`（当 `count > 0` 时）
* `count < 0`
* `sources` 中存在 `NULL` 指针或 `data == NULL` 的表
* 扩容失败（内存分配失败）

**失败时**：必须返回 `ERROR` 且**不得修改 dest**（包括 `dest->length`、`dest->capacity` 和 `dest->data` 的内容）

**特殊情况**：

* `count == 0`：清空 `dest`，返回 `OK`
* `sources` 中包含空表：正常合并，空表不贡献元素

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `MergeLists` 需要先计算总元素个数，然后扩容（如需），最后依次复制各表元素。
3. **失败不修改 dest 是强约束**：参数非法或扩容失败时不得改变 `dest->length`、`dest->capacity` 和 `dest->data` 的内容。
4. **扩容策略**：容量不足时，建议按 2 倍扩容，直到能容纳所有元素。
5. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

---

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 合并表的个数：0 ≤ count ≤ 100
* 每个表长度：0 ≤ length ≤ 1000
* 测试会覆盖：

  * 合并 0 个表（清空目标表）
  * 合并包含空表的情况
  * 需要多次扩容
  * sources 中存在 NULL 指针
  * NULL 安全性验证
  * 失败不修改 dest

---

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `MergeLists` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   MergeLists(SeqList* dest, SeqList** sources, int count);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3720-弹性删除区间

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**弹性删除区间**操作。

## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

给定位置区间 `[start, end]`（包含端点），删除顺序表中该区间内的所有元素，并保持其余元素相对顺序不变：

* 删除位置必须合法：`0 <= start <= end < length`
* 删除后，`end` 之后的元素前移
* 删除后可选择性收缩容量以节省空间

若参数非法，操作失败：必须返回 `ERROR` 且不得修改顺序表。

---


## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）弹性删除区间 —— **本题核心**

```c
Status DeleteRange(SeqList* L, int start, int end, int shrink);
```

**功能要求**：

* 删除位置区间 `[start, end]` 内的所有元素（包含端点）
* 删除后，`end` 之后的元素前移到 `start` 位置
* 若 `shrink != 0` 且删除后 `length <= capacity / 4`，收缩容量为 `capacity / 2`（但不小于初始容量 10）
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* `start < 0` 或 `end >= L->length`
* `start > end`
* 收缩容量时内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改 L**（包括 `L->length`、`L->capacity` 和 `L->data` 的内容）

**特殊情况**：

* 删除整个表（`start == 0 && end == length - 1`）：`length` 变为 0
* `shrink == 0`：不收缩容量

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `DeleteRange` 推荐使用 `memmove` 或循环实现元素前移，时间复杂度 O(n)。
3. **失败不修改 L 是强约束**：参数非法或收缩失败时不得改变 `L->length`、`L->capacity` 和 `L->data` 的内容。
4. **收缩策略**：仅当 `shrink != 0` 且 `length <= capacity / 4` 时收缩为 `capacity / 2`，但不小于 10。
5. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

---

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 删除区间：0 ≤ start ≤ end < length
* 测试会覆盖：

  * 删除头部、中间、尾部
  * 删除单个元素（start == end）
  * 删除整个表
  * 需要收缩容量
  * 不收缩容量（shrink == 0）
  * 区间非法
  * NULL 安全性验证
  * 失败不修改 L

---

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `DeleteRange` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   DeleteRange(SeqList* L, int start, int end, int shrink);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3721-去重压缩

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**去重并压缩**操作。

## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---
给定一个**非降序**顺序表（允许重复），删除所有重复元素，使每个值只保留一次，并压缩容量以节省空间：

* 输入保证顺序表已按非降序排列
* 去重后保持非降序
* 去重后收缩容量为实际长度（但不小于初始容量 10）

若参数非法或内存分配失败，操作失败：必须返回 `ERROR` 且不得修改顺序表。

---


## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）去重并压缩 —— **本题核心**

```c
Status UniqueAndCompress(SeqList* L);
```

**功能要求**：

* 删除所有重复元素，每个值只保留一次
* 假设 L 已按非降序排列（无需验证）
* 去重后，收缩容量为实际长度（但不小于 10）
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* 收缩容量时内存分配失败

**失败时**：必须返回 `ERROR` 且**不得修改 L**（包括 `L->length`、`L->capacity` 和 `L->data` 的内容）

**特殊情况**：

* 空表：返回 `OK`，不修改
* 无重复元素：仍需压缩容量
* 全部重复：去重后只剩 1 个元素

**算法提示**：

* 推荐使用"双指针"算法：一个指针遍历，一个指针记录写入位置
* 时间复杂度 O(n)

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `UniqueAndCompress` 推荐使用"双指针"算法实现去重，时间复杂度 O(n)。
3. **失败不修改 L 是强约束**：收缩失败时不得改变 `L->length`、`L->capacity` 和 `L->data` 的内容。
4. **压缩策略**：去重后收缩容量为 `max(length, 10)`。
5. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

---

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 输入保证非降序
* 测试会覆盖：

  * 空表
  * 无重复元素
  * 全部重复
  * 部分重复
  * 大量重复导致压缩比高
  * NULL 安全性验证
  * 失败不修改 L

---

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `UniqueAndCompress` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   UniqueAndCompress(SeqList* L);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3722-区间统计与修改

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**区间统计与修改**操作。
## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

给定位置区间 `[start, end]`（包含端点）和条件值 `threshold`，完成以下操作：

* 统计区间内满足 `data[i] >= threshold` 的元素个数
* 将这些满足条件的元素值修改为 `newValue`
* 返回统计的元素个数

若参数非法，操作失败：必须返回 `-1` 且不得修改顺序表。

---


## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）区间统计与修改 —— **本题核心**

```c
int CountAndUpdate(SeqList* L, int start, int end, ElementType threshold, ElementType newValue);
```

**功能要求**：

* 统计区间 `[start, end]` 内满足 `data[i] >= threshold` 的元素个数
* 将这些满足条件的元素修改为 `newValue`
* 返回统计的元素个数

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* `start < 0` 或 `end >= L->length`
* `start > end`

**失败时**：必须返回 `-1` 且**不得修改 L**

**特殊情况**：

* 区间内无满足条件的元素：返回 `0`，不修改任何元素
* 区间内全部满足条件：返回 `end - start + 1`，全部修改

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `CountAndUpdate` 推荐使用单次遍历实现，时间复杂度 O(n)。
3. **失败不修改 L 是强约束**：参数非法时不得改变 `L->data` 的内容。
4. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

---

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 区间范围：0 ≤ start ≤ end < length
* threshold 和 newValue 为任意 `int` 值
* 测试会覆盖：

  * 全部满足条件
  * 部分满足条件
  * 全部不满足条件
  * 单元素区间
  * 整个表作为区间
  * 区间非法
  * NULL 安全性验证

---

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `CountAndUpdate` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
int      CountAndUpdate(SeqList* L, int start, int end, ElementType threshold, ElementType newValue);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3723-循环左移

### 问题描述

本题要求你实现**动态扩容顺序表**（动态数组），并在此基础上完成**循环左移**操作。
## 顺序表类型定义

```c
typedef int ElementType;

typedef struct {
    ElementType* data;  // 动态数组
    int length;         // 当前长度
    int capacity;       // 当前容量
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

给定非负整数 k，将顺序表循环左移 k 位：

* 前 k 个元素移到表尾
* 保持相对顺序不变
* k 可能大于表长，需要取模处理

若参数非法，操作失败：必须返回 `ERROR` 且不得修改顺序表。

---


## 函数说明

你需要实现以下 **5 个函数**：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

**功能要求**：

* 分配 `SeqList` 结构体
* 分配初始容量为 10 的 `data` 数组
* 初始化 `length = 0`，`capacity = 10`
* 失败返回 `NULL`

---

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

**功能要求**：

* 若 `L == NULL`，返回 `ERROR`
* 若 `length == capacity`，扩容为原来的 2 倍
* 在表尾插入元素 `x`
* 成功返回 `OK`，失败返回 `ERROR`

---

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

**功能要求**：

* 若 `L == NULL` 或 `L->data == NULL`，输出 `-1` 并换行
* 否则按格式输出：

  * 空表：输出空行（直接换行）
  * 非空表：输出所有元素，每个元素后跟一个空格，最后换行

---

### 4）循环左移 —— **本题核心**

```c
Status RotateLeft(SeqList* L, int k);
```

**功能要求**：

* 将顺序表循环左移 k 位
* 若 `k > L->length`，需要取模处理：`k = k % L->length`
* 若 `k == 0` 或 `L->length <= 1`，不做任何操作，返回 `OK`
* 成功返回 `OK`

**失败条件**（任一满足即失败）：

* `L == NULL` 或 `L->data == NULL`
* `k < 0`

**失败时**：必须返回 `ERROR` 且**不得修改 L**

**特殊情况**：

* 空表：返回 `OK`，不修改
* k == 0：返回 `OK`，不修改
* k == length：循环一整圈，返回 `OK`，不修改

**算法提示**（三次翻转法）：

1. 翻转前 k 个元素：`[1 2 3] 4 5 6 7 8` → `[3 2 1] 4 5 6 7 8`
2. 翻转后 n-k 个元素：`3 2 1 [4 5 6 7 8]` → `3 2 1 [8 7 6 5 4]`
3. 翻转整个数组：`[3 2 1 8 7 6 5 4]` → `4 5 6 7 8 1 2 3`

---

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

**功能要求**：

* 若 `pL == NULL` 或 `*pL == NULL`，直接返回
* 释放 `(*pL)->data`
* 释放 `*pL`
* 将 `*pL` 置为 `NULL`
* 支持重复销毁（不崩溃）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

1. **必须使用本题给定的顺序表结构与接口**完成任务，不允许使用 STL 容器替代顺序表存储。
2. `RotateLeft` 推荐使用"三次翻转法"实现，时间复杂度 O(n)，空间复杂度 O(1)。
3. **失败不修改 L 是强约束**：参数非法时不得改变 `L->data` 的内容。
4. **k 的处理**：必须正确处理 `k > length` 的情况。
5. **内存安全**：所有动态分配的内存必须正确释放，避免内存泄漏。

---

### 【数据范围】

* 初始容量 = 10
* 元素值为 `int`，可能包含负数
* 0 ≤ k ≤ 10000
* 测试会覆盖：

  * k == 0（不移动）
  * k == length（循环一整圈）
  * k > length（需取模）
  * k == 1（左移1位）
  * 空表
  * 单元素表
  * k 为负数（非法）
  * NULL 安全性验证

---

### 【提交说明】

* **只需提交 5 个函数**：`InitList` / `PushBack` / `PrintList` / `RotateLeft` / `DestroyList`
* **不要提交** `main`，不要重复定义结构体/宏/其它函数
* **不要输出任何提示性文字**（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef int ElementType;

typedef struct {
    ElementType* data;
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   RotateLeft(SeqList* L, int k);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---
