# 作业11 带权邻接表无向图最小生成树

> 来源：[课程题集](https://10-132-246-246.webvpn.wzu.edu.cn/course/7/volume/746/detail/)

## 目录

1. [4091-创建图](#4091-创建图)
2. [4092-创建带权邻接表节点](#4092-创建带权邻接表节点)
3. [4093-添加带权边](#4093-添加带权边)
4. [4094-删除边](#4094-删除边)
5. [4095-判断边是否存在](#4095-判断边是否存在)
6. [4096-获取边的权重](#4096-获取边的权重)
7. [4097-获取顶点的度](#4097-获取顶点的度)
8. [4098-获取邻居列表](#4098-获取邻居列表)
9. [4099-获取边数](#4099-获取边数)
10. [4100-获取顶点数](#4100-获取顶点数)
11. [4101-深度优先遍历标记](#4101-深度优先遍历标记)
12. [4102-判断图是否连通](#4102-判断图是否连通)
13. [4103-获取连通分量数](#4103-获取连通分量数)
14. [4104-获取最小权重边](#4104-获取最小权重边)
15. [4105-并查集查找](#4105-并查集查找)
16. [4106-并查集合并](#4106-并查集合并)
17. [4109-获取 MST 总权重](#4109-获取mst总权重)
18. [4107-Kruskal 算法](#4107-kruskal算法)
19. [4108-Prim 算法](#4108-prim算法)
20. [4110-销毁图](#4110-销毁图)

---

## 4091-创建图

### 问题描述

本题要求你实现**带权邻接表无向图的初始化操作**。

本题使用**带权邻接表**表示无向图。每个顶点维护一条链表，链表中每个节点存储一个邻居顶点编号及对应的边权重。`CreateGraph` 负责分配并初始化一张空图。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
WALGraph* CreateGraph(int size);
```

**参数：**
- `size`：顶点数量

**返回值：**
- 成功：返回新创建的图指针
- 失败（`size <= 0` 或内存分配失败）：返回 `NULL`

**初始状态要求：**
- `size` 初始化为 `size`，`edgeCount` 初始化为 `0`
- `heads` 数组长度为 `size`，每个元素初始化为 `NULL`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `CreateGraph`，不要提交 `main` 函数

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

WALGraph* CreateGraph(int size);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4092-创建带权邻接表节点

### 问题描述

本题要求你实现**带权邻接表节点的创建操作**。

每个节点存储一个邻居顶点编号和对应边的权重，`next` 指针串成链表。

---

## 数据结构定义

```c
typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
WAdjNode* CreateWAdjNode(int vertex, int weight);
```

**参数：**
- `vertex`：邻居顶点编号
- `weight`：边的权重

**返回值：**
- 成功：返回新创建的节点指针；失败（内存分配失败）：返回 `NULL`

**初始状态：** `vertex` 和 `weight` 初始化为参数值，`next` 初始化为 `NULL`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `CreateWAdjNode`

### 预置代码

```c
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

WAdjNode* CreateWAdjNode(int vertex, int weight);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4093-添加带权边

### 问题描述

本题要求你实现**带权邻接表无向图的添加边操作（头插）**。

在无向图中添加权重为 `w` 的边 `(u, v)`，在顶点 `u` 和顶点 `v` 的邻接链表头部各插入一个节点，并将 `edgeCount` 加一。若边已存在则不重复添加。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
Status AddEdge(WALGraph* g, int u, int v, int w);
```

**参数：**
- `g`：图指针；`u`、`v`：端点编号；`w`：权重（必须为正整数）

**返回值：**
- `OK`：成功添加；`ERROR`：参数非法、边已存在或 `g == NULL`

**特殊情况：**
- `g == NULL`、`u`/`v` 越界、`u == v`、`w <= 0`、边已存在 → 返回 `ERROR`

**失败时不得修改结构内容。**

**依赖接口（由系统提供）：**
- `WAdjNode* CreateWAdjNode(int vertex, int weight)`
- `int HasEdge(WALGraph* g, int u, int v)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `AddEdge`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

WAdjNode* CreateWAdjNode(int vertex, int weight);
int       HasEdge(WALGraph* g, int u, int v);
Status    AddEdge(WALGraph* g, int u, int v, int w);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4094-删除边

### 问题描述

本题要求你实现**带权邻接表无向图的删除边操作**。

从顶点 `u` 和顶点 `v` 的邻接链表中分别删除对方的节点，并将 `edgeCount` 减一。边不存在时返回 `ERROR`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
Status RemoveEdge(WALGraph* g, int u, int v);
```

**返回值：**
- `OK`：成功删除；`ERROR`：边不存在、参数非法或 `g == NULL`

**失败时不得修改结构内容。**

**依赖接口（由系统提供）：**
- `int HasEdge(WALGraph* g, int u, int v)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `RemoveEdge`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int    HasEdge(WALGraph* g, int u, int v);
Status RemoveEdge(WALGraph* g, int u, int v);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4095-判断边是否存在

### 问题描述

本题要求你实现**判断带权邻接表无向图中两顶点间是否存在边的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int HasEdge(WALGraph* g, int u, int v);
```

**参数：**
- `g`：图指针
- `u`、`v`：待查询的两个顶点编号

**返回值：** `1`：边存在；`0`：边不存在、参数非法或 `g == NULL`

**特殊情况：**
- `g == NULL` → 返回 `0`
- `u` 或 `v` 越界（`< 0` 或 `>= g->size`）→ 返回 `0`
- `u == v`（自环）→ 返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `HasEdge`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int HasEdge(WALGraph* g, int u, int v);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4096-获取边的权重

### 问题描述

本题要求你实现**返回带权邻接表无向图中两顶点间边的权重的操作**。边不存在时返回 `0`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetWeight(WALGraph* g, int u, int v);
```

**参数：**
- `g`：图指针
- `u`、`v`：边的两个端点编号

**返回值：** 边的权重；边不存在、参数非法或 `g == NULL` 时返回 `0`

**特殊情况：**
- `g == NULL` → 返回 `0`
- `u` 或 `v` 越界 → 返回 `0`
- `u == v` → 返回 `0`
- 边不存在 → 返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetWeight`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetWeight(WALGraph* g, int u, int v);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4097-获取顶点的度

### 问题描述

本题要求你实现**返回带权邻接表无向图中指定顶点度数的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetDegree(WALGraph* g, int v);
```

**参数：**
- `g`：图指针
- `v`：顶点编号

**返回值：** 顶点 `v` 的度；`g == NULL` 或 `v` 越界时返回 `-1`

**特殊情况：**
- `g == NULL` → 返回 `-1`
- `v < 0` 或 `v >= g->size` → 返回 `-1`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetDegree`

### 预置代码

```c
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetDegree(WALGraph* g, int v);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4098-获取邻居列表

### 问题描述

本题要求你实现**返回带权邻接表无向图中指定顶点所有邻居的操作**。邻居编号按升序写入输出数组。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetNeighbors(WALGraph* g, int v, int* neighbors);
```

**参数：**
- `g`：图指针
- `v`：顶点编号
- `neighbors`：输出数组，由调用方分配，长度不小于 `g->size`

**返回值：** 邻居数量；`g == NULL`、`v` 越界或 `neighbors == NULL` 时返回 `-1`

**特殊情况：**
- `g == NULL` 或 `neighbors == NULL` → 返回 `-1`
- `v < 0` 或 `v >= g->size` → 返回 `-1`
- 顶点无邻居 → 返回 `0`，`neighbors` 内容不写入

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetNeighbors`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetNeighbors(WALGraph* g, int v, int* neighbors);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4099-获取边数

### 问题描述

本题要求你实现**返回带权邻接表无向图中当前边数的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetEdgeCount(WALGraph* g);
```

**参数：**
- `g`：图指针

**返回值：** 当前边数；`g == NULL` 时返回 `0`

**特殊情况：**
- `g == NULL` → 返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 预置代码

```c
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetEdgeCount(WALGraph* g);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4100-获取顶点数

### 问题描述

本题要求你实现**返回带权邻接表无向图中顶点数量的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetVertexCount(WALGraph* g);
```

**参数：**
- `g`：图指针

**返回值：** 顶点数；`g == NULL` 时返回 `0`

**特殊情况：**
- `g == NULL` → 返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetVertexCount`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetVertexCount(WALGraph* g);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4101-深度优先遍历标记

### 问题描述

本题要求你实现**从指定顶点出发进行深度优先遍历并标记所有可达顶点的操作**。

从顶点 `start` 出发，沿邻接链表中的边递归（或迭代）访问所有可达顶点，将每个访问到的顶点在 `visited` 数组中标记为 `1`。`visited` 数组由调用方分配并初始化为全 `0`，本函数只负责标记。

`DFSVisit` 是 `IsConnected` 和 `GetConnectedComponents` 的基础，不输出遍历序列，只做标记。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
void DFSVisit(WALGraph* g, int start, int* visited);
```

**参数：**
- `g`：图指针
- `start`：起始顶点编号
- `visited`：标记数组，长度不小于 `g->size`，由调用方保证，初始值全为 `0`

**操作要求：**
- 将从 `start` 出发所有可达顶点（包括 `start` 本身）在 `visited` 中置为 `1`
- 不重复访问已标记的顶点
- `g == NULL`、`start` 越界或 `visited == NULL` 时直接返回

**依赖接口（由系统提供，无需实现）：**
- `int GetNeighbors(WALGraph* g, int v, int* neighbors)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `DFSVisit`
- 不要提交 `main` 函数

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int  GetNeighbors(WALGraph* g, int v, int* neighbors);
void DFSVisit(WALGraph* g, int start, int* visited);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4102-判断图是否连通

### 问题描述

本题要求你实现**判断带权无向图是否连通的操作**。从顶点 `0` 出发进行一次 DFS，若能访问到所有顶点则图连通，返回 `1`，否则返回 `0`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int IsConnected(WALGraph* g);
```

**返回值：** `1`：连通；`0`：不连通、`g == NULL` 或 `g->size == 0`

**特殊情况：** `g->size == 1` 返回 `1`

**依赖接口（由系统提供）：**
- `void DFSVisit(WALGraph* g, int start, int* visited)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `IsConnected`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int  GetNeighbors(WALGraph* g, int v, int* neighbors);
void DFSVisit(WALGraph* g, int start, int* visited);
int  IsConnected(WALGraph* g);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4103-获取连通分量数

### 问题描述

本题要求你实现**返回带权无向图连通分量数量的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetConnectedComponents(WALGraph* g);
```

**返回值：** 连通分量数；`g == NULL` 或 `g->size == 0` 时返回 `0`

**依赖接口（由系统提供）：**
- `void DFSVisit(WALGraph* g, int start, int* visited)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetConnectedComponents`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

void DFSVisit(WALGraph* g, int start, int* visited);
int  GetConnectedComponents(WALGraph* g);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4104-获取最小权重边

### 问题描述

本题要求你实现**返回带权邻接表无向图中权重最小的边的操作**。

**Tie-breaking 规则**：权重相同时，优先选 `min(u,v)` 较小的边，再比较 `max(u,v)`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetMinEdge(WALGraph* g, int* u, int* v, int* w);
```

**参数：** `u`、`v`：输出端点（保证 `*u < *v`）；`w`：输出权重

**返回值：** `1`：成功；`0`：无边、`g == NULL` 或输出参数为 `NULL`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `GetMinEdge`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetMinEdge(WALGraph* g, int* u, int* v, int* w);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4105-并查集查找

### 问题描述

本题要求你实现**并查集的查找操作（路径压缩）**。

并查集用于 Kruskal 算法判断加入某条边是否会形成环。`UFFind` 返回元素 `x` 所在集合的根节点，并在查找过程中使用**路径压缩**优化，将路径上所有节点直接连接到根节点。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct {
    int* parent;  /* parent[i] 为节点 i 的父节点，根节点的 parent 为自身 */
    int* rank;    /* rank[i] 为节点 i 的秩，用于按秩合并 */
    int  size;    /* 元素总数 */
} UnionFind;
```

---

## 函数说明

```c
int UFFind(UnionFind* uf, int x);
```

**参数：**
- `uf`：并查集指针
- `x`：待查找的元素

**返回值：**
- 元素 `x` 所在集合的根节点编号
- `uf == NULL` 或 `x` 越界时返回 `-1`

**操作要求：**
- 使用递归路径压缩：`uf->parent[x] = UFFind(uf, uf->parent[x])`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `UFFind`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct {
    int* parent;
    int* rank;
    int  size;
} UnionFind;

int UFFind(UnionFind* uf, int x);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4106-并查集合并

### 问题描述

本题要求你实现**并查集的合并操作（按秩合并）**。

将元素 `x` 和元素 `y` 所在的集合合并为一个集合。使用**按秩合并**策略：将秩较小的树挂到秩较大的树的根节点下；若秩相等，任选一方作为新根并将其秩加一。

若 `x` 和 `y` 已在同一集合，返回 `ERROR`，不做修改。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct {
    int* parent;
    int* rank;
    int  size;
} UnionFind;
```

---

## 函数说明

```c
Status UFUnion(UnionFind* uf, int x, int y);
```

**返回值：**
- `OK`：成功合并两个不同集合
- `ERROR`：`x` 和 `y` 已在同一集合，或 `uf == NULL`，或参数越界

**依赖接口（由系统提供）：**
- `int UFFind(UnionFind* uf, int x)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `UFUnion`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct {
    int* parent;
    int* rank;
    int  size;
} UnionFind;

int    UFFind(UnionFind* uf, int x);
Status UFUnion(UnionFind* uf, int x, int y);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4109-获取 MST 总权重

### 问题描述

本题要求你实现**对给定 MST 边集求总权重的操作**。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int GetMSTWeight(int edges[][3], int cnt);
```

**参数：**
- `edges`：边集数组，`edges[i][0]`、`edges[i][1]` 为端点，`edges[i][2]` 为权重
- `cnt`：边的数量

**返回值：** 所有边权重之和；`edges == NULL` 或 `cnt <= 0` 时返回 `0`

**特殊情况：**
- `edges == NULL` 或 `cnt <= 0` → 返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 数据范围

- `cnt` 满足 `0 <= cnt <= V-1`，其中 `V` 为图的顶点数
- 每条边权重为正整数

---

## 提交说明

- 只需提交函数 `GetMSTWeight`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetMSTWeight(int edges[][3], int cnt);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4107-Kruskal 算法

### 问题描述

本题要求你实现**基于带权邻接表的 Kruskal 最小生成树算法**。

**Tie-breaking 规则**：权重相同时，优先选择 `min(u,v)` 较小的边，再比较 `max(u,v)`。

图不连通时返回 `-1`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

typedef struct {
    int* parent;
    int* rank;
    int  size;
} UnionFind;
```

---

## 函数说明

```c
int Kruskal(WALGraph* g, int edges[][3], int* cnt);
```

**参数：**
- `edges`：输出数组，`edges[i] = {u, v, w}`
- `cnt`：输出参数，写入 MST 边数

**返回值：** MST 总权重；图不连通或 `g == NULL` 时返回 `-1`

**依赖接口（由系统提供）：**
- `int UFFind(UnionFind* uf, int x)`
- `Status UFUnion(UnionFind* uf, int x, int y)`
- `int GetMSTWeight(int edges[][3], int cnt)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `Kruskal`

### 预置代码

```c
#include <bits/stdc++.h>
using namespace std;

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

typedef struct {
    int* parent;
    int* rank;
    int  size;
} UnionFind;

int    UFFind(UnionFind* uf, int x);
Status UFUnion(UnionFind* uf, int x, int y);
int    GetMSTWeight(int edges[][3], int cnt);
int    Kruskal(WALGraph* g, int edges[][3], int* cnt);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4108-Prim 算法

### 问题描述

本题要求你实现**基于带权邻接表的 Prim 最小生成树算法**。

**Tie-breaking 规则**：权重相同时，优先将编号较小的顶点加入 MST；若编号也相同，优先选择来自编号较小的 MST 内顶点的边。

图不连通时返回 `-1`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
int Prim(WALGraph* g, int edges[][3], int* cnt);
```

**返回值：** MST 总权重；图不连通或 `g == NULL` 时返回 `-1`

**依赖接口（由系统提供）：**
- `int GetMSTWeight(int edges[][3], int cnt)`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `Prim`

### 预置代码

```c
#include <bits/stdc++.h>
using namespace std;

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

int GetMSTWeight(int edges[][3], int cnt);
int Prim(WALGraph* g, int edges[][3], int* cnt);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---

## 4110-销毁图

### 问题描述

本题要求你实现**带权邻接表无向图的销毁操作**。释放图占用的所有内存，并将外部指针置为 `NULL`。

---

## 数据结构定义

```c
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;
```

---

## 函数说明

```c
void DestroyGraph(WALGraph** pg);
```

**操作要求：**
- `pg == NULL` 或 `*pg == NULL` 时直接返回，不做任何操作
- 遍历每条邻接链表，逐节点 `free`
- 释放头指针数组 `(*pg)->heads`
- 释放图结构体 `*pg`
- 将 `*pg` 置为 `NULL`，防止悬空指针

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

## 提交说明

- 只需提交函数 `DestroyGraph`

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct WAdjNode {
    int              vertex;
    int              weight;
    struct WAdjNode* next;
} WAdjNode;

typedef struct {
    WAdjNode** heads;
    int        size;
    int        edgeCount;
} WALGraph;

void DestroyGraph(WALGraph** pg);

int main() { return 0; }

/******************** 学生提交的函数将被拼接在此处 ********************/
```

---
