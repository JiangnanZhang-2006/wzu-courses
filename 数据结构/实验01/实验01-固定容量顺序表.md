# 实验01 固定容量顺序表

> 来源：[课程题集](https://10-132-246-246.webvpn.wzu.edu.cn/course/7/volume/725/detail/)

## 目录

1. [3682-创建与初始化](#3682-创建与初始化)
2. [3683-表尾插入](#3683-表尾插入)
3. [3684-输出顺序表](#3684-输出顺序表)
4. [3685-判空](#3685-判空)
5. [3686-判满](#3686-判满)
6. [3687-获取长度](#3687-获取长度)
7. [3688-按位置取值](#3688-按位置取值)
8. [3689-按值查找](#3689-按值查找)
9. [3690-指定位置插入](#3690-指定位置插入)
10. [3691-删除指定位置](#3691-删除指定位置)
11. [3692-删除所有指定值](#3692-删除所有指定值)
12. [3693-逆置顺序表](#3693-逆置顺序表)
13. [3694-清空顺序表](#3694-清空顺序表)
14. [3695-销毁顺序表](#3695-销毁顺序表)
15. [3696-区间删除](#3696-区间删除)
16. [3697-有合并序](#3697-有合并序)
17. [3698-去重](#3698-去重)
18. [3699-循环右移](#3699-循环右移)
19. [3700-批处理操作模拟](#3700-批处理操作模拟)
20. [3701-有序原地去重](#3701-有序原地去重)
21. [3702-有序合并去重](#3702-有序合并去重)

---

## 3682-创建与初始化

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现**创建与初始化**操作。

给定**固定容量顺序表**的类型定义如下：

```c
typedef int ElementType;

#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度（已存元素个数）
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

设计 `SeqList* InitList(void)` 函数，实现**创建并初始化**一个空的顺序表。

> 说明：本题测试会进行“创建→插入→输出”等实际使用验证，而不是只检查指针是否为空。

---

## 函数说明

**函数原型：**

```c
SeqList* InitList(void);
```

**参数说明：**

* 无

**返回值说明：**

* 创建成功：返回指向新顺序表的指针
* 创建失败（内存申请失败）：返回 `NULL`

**特殊情况处理：**

* 创建后必须满足：

  * `length == 0`
  * `capacity == MAXSIZE`
* 若返回 `NULL`，测试代码会按约定处理（你无需输出任何提示信息）。

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】
* 顺序表容量：`MAXSIZE = 1000`
* 初始化后元素个数：`length = 0`
* 元素类型：`ElementType = int`
  （本题仅初始化，不涉及元素值范围）
【提交说明】
* 本题有预置代码，**只需提交 `InitList` 函数的实现**。
* 不要提交 `main` 函数或其它辅助函数。
* 不要重复提交预置代码中已经给出的结构体定义与宏。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

/******************** 数据结构定义（与题面完全一致） ********************/
typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

/******************** 函数声明（本题要求实现） ********************/
SeqList* InitList(void);

/******************** 主函数 ********************/
int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数
     * 4. 输出测试结果
     * 5. 清理资源（销毁数据结构）
     *
     * 你无需关心此处代码的具体实现细节。
     */
    return 0;
}

/******************** 函数实现 ********************/
/*
 * 请在下方实现题目要求的函数
 * 你的代码将被添加在此处
 */
```

---

## 3683-表尾插入

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现**表尾插入**操作。

顺序表类型定义如下：

```c
typedef int ElementType;

#define MAXSIZE 1000
typedef enum { OK = 1, ERROR = 0 } Status;

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 `PushBack`：把元素 `x` 插入到顺序表表尾。

---

## 函数说明

**函数原型：**

```c

Status PushBack(SeqList* L, ElementType x);
```

**参数说明：**

* `L`：顺序表指针
* `x`：要插入的元素

**返回值说明：**

* 返回 `OK`：插入成功
* 返回 `ERROR`：插入失败（以下任一情况）

  * `L == NULL`
  * 顺序表已满（`length >= capacity`）

**功能要求：**

* 成功插入时：

  * 将 `x` 写入 `data[length]`
  * `length` 自增 1
* 失败时：

  * 不修改顺序表内容与 `length`

---

【数据范围】
* `MAXSIZE = 1000`
* `x` 为 `int`，范围按平台 `int` 范围即可（测试会覆盖负数、重复值等情况）
* 测试会包含：
  * 空表尾插、多次尾插
  * 接近满表与满表插入失败

【提交说明】
* 只需提交 `PushBack` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。
* 评测时会用统一测试流程反复调用尾插，并结合输出表内容、长度、判满等验证正确性。

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】
* `MAXSIZE = 1000`
* `x` 为 `int`，范围按平台 `int` 范围即可（测试会覆盖负数、重复值等情况）
* 测试会包含：
  * 空表尾插、多次尾插
  * 接近满表与满表插入失败

【提交说明】
* 只需提交 `PushBack` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。
* 评测时会用统一测试流程反复调用尾插，并结合输出表内容、长度、判满等验证正确性。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

Status PushBack(SeqList* L, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 调用你实现的函数（可能多次）
     * 3. 输出测试结果
     * 4. 清理资源
     */
    return 0;
}
```

---

## 3684-输出顺序表

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现**输出**操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **PrintList**：按顺序输出顺序表中所有元素。

---

## 函数说明

**函数原型：**

```c
void PrintList(const SeqList* L);
```

**参数说明：**

* `L`：顺序表指针（只读）

**返回值说明：**

* 无返回值

**功能要求：**

* 当 `L == NULL` 时：输出一个换行（即输出空行）。
* 当 `L != NULL` 时：

  * 按顺序输出 `data[0] ~ data[length-1]` 的元素
  * 元素之间用**一个空格**分隔
  * 行末输出换行
* 若 `length == 0`：输出空行（只输出换行）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 提示

【输出格式要求】

* `L == NULL`：输出空行
* `length == 0`：输出空行
* `length > 0`：输出形如：

  * `a0 a1 a2 ... a(length-1)`（空格分隔，末尾换行）
* **不允许**多输出空格（例如行首空格、行尾多余空格），建议用 “先输出第一个，再输出其余前置空格” 的方式实现。

【数据范围】

* `MAXSIZE = 1000`
* `ElementType = int`
* 测试会包含：

  * `L == NULL`
  * 空表输出
  * 单元素输出
  * 多元素输出（含负数、重复值）

【提交说明】

* 只需提交 `PrintList` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。
* 评测将使用统一测试流程多次调用输出函数，并与标准输出严格比对。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

void PrintList(const SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据
     * 3. 调用你实现的函数（可能多次）
     * 4. 输出测试结果
     * 5. 清理资源
     */
    return 0;
}
```

---

## 3685-判空

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现判空操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **IsEmpty**：判断顺序表是否为空。

---

## 函数说明

函数原型：

```c
int IsEmpty(const SeqList* L);
```

参数说明：

* `L`：顺序表指针（只读）

返回值说明：

* 返回 `1`：顺序表为空（或 `L == NULL`）
* 返回 `0`：顺序表非空

功能要求：

* 当 `L == NULL` 时，视为空表，返回 `1`
* 当 `L != NULL` 时：

  * 若 `length == 0` 返回 `1`
  * 否则返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * `L == NULL`
  * 空表判空
  * 非空表判空
  * 清空/删除到空后再次判空

【提交说明】

* 只需提交 `IsEmpty` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

int IsEmpty(const SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入/删除测试数据（可能多次）
     * 3. 调用你实现的函数（可能多次）
     * 4. 输出测试结果
     * 5. 清理资源
     */
    return 0;
}
```

---

## 3686-判满

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现判满操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **IsFull**：判断顺序表是否已满。

---

## 函数说明

函数原型：

```c
int IsFull(const SeqList* L);
```

参数说明：

* `L`：顺序表指针（只读）

返回值说明：

* 返回 `1`：顺序表已满
* 返回 `0`：顺序表未满（或 `L == NULL`）

功能要求：

* 当 `L == NULL` 时返回 `0`
* 当 `L != NULL` 时：

  * 若 `length >= capacity` 返回 `1`
  * 否则返回 `0`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 未满判满
  * 恰满判满
  * 清空后判满（应恢复为未满）

【提交说明】

* 只需提交 `IsFull` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

int IsFull(const SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建和初始化数据结构
     * 2. 插入测试数据（可能插到满表）
     * 3. 调用你实现的函数（可能多次）
     * 4. 输出测试结果
     * 5. 清理资源
     */
    return 0;
}
```

---

## 3687-获取长度

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现获取长度操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **GetLength**：返回顺序表当前元素个数。

---

## 函数说明

函数原型：

```c
int GetLength(const SeqList* L);
```

参数说明：

* `L`：顺序表指针（只读）

返回值说明：

* 返回当前顺序表长度 `length`
* 若 `L == NULL`，返回 `0`

功能要求：

* `L == NULL`：返回 `0`
* `L != NULL`：返回 `length`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 空表长度
  * 插入后长度变化
  * 删除/清空后长度变化

【提交说明】

* 只需提交 `GetLength` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

int GetLength(const SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 多次插入/删除数据
     * 2. 调用你实现的函数获取长度（可能多次）
     * 3. 输出测试结果
     */
    return 0;
}
```

---

## 3688-按位置取值

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现按位置取值操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **GetElem**：获取顺序表中指定位置的元素值。

---

## 函数说明

函数原型：

```c
typedef enum { OK = 1, ERROR = 0 } Status;

Status GetElem(const SeqList* L, int pos, ElementType* out);
```

参数说明：

* `L`：顺序表指针（只读）
* `pos`：要获取的位置（**1-based**，即第 1 个元素位置为 1）
* `out`：用于保存取到的元素值（输出参数）

返回值说明：

* 返回 `OK`：获取成功，结果写入 `*out`
* 返回 `ERROR`：获取失败（以下任一情况）

  * `L == NULL`
  * `out == NULL`
  * `pos < 1` 或 `pos > length`

功能要求：

* 成功时：`*out = data[pos-1]`
* 失败时：不修改 `*out`（测试代码会自行处理失败输出）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【位置说明】

* `pos` 采用 **1-based** 下标：

  * 第一个元素：`pos = 1`
  * 最后一个元素：`pos = length`

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * `pos = 1`、`pos = length`、中间位置
  * `pos = 0`、`pos = length+1` 等非法情况
  * 空表取值

【提交说明】

* 只需提交 `GetElem` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

Status GetElem(const SeqList* L, int pos, ElementType* out);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据
     * 2. 多次调用 GetElem（含合法/非法位置）
     * 3. 输出测试结果
     */
    return 0;
}
```

---

## 3689-按值查找

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现按值查找操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **LocateElem**：查找顺序表中第一个等于 `x` 的元素位置。

---

## 函数说明

函数原型：

```c
int LocateElem(const SeqList* L, ElementType x);
```

参数说明：

* `L`：顺序表指针（只读）
* `x`：要查找的元素值

返回值说明：

* 若查找成功：返回元素首次出现的位置（**1-based**）
* 若查找失败：返回 `-1`
* 若 `L == NULL`：返回 `-1`

功能要求：

* 从前到后遍历顺序表
* 返回第一个满足 `data[i] == x` 的位置 `i+1`
* 若不存在则返回 `-1`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【位置说明】

* 返回位置为 **1-based**
* 若有重复元素，必须返回**第一次出现**的位置

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 查找存在值
  * 查找不存在值
  * 查找重复值（验证返回首次位置）
  * `L == NULL`

【提交说明】

* 只需提交 `LocateElem` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

int LocateElem(const SeqList* L, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据（可能含重复元素）
     * 2. 多次调用 LocateElem（含存在/不存在）
     * 3. 输出测试结果
     */
    return 0;
}
```

---

## 3690-指定位置插入

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现指定位置插入操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **InsertAt**：将元素 `x` 插入到顺序表的第 `pos` 个位置。

---

## 函数说明

函数原型：

```c
typedef enum { OK = 1, ERROR = 0 } Status;

Status InsertAt(SeqList* L, int pos, ElementType x);
```

参数说明：

* `L`：顺序表指针
* `pos`：插入位置（**1-based**）
* `x`：要插入的元素

返回值说明：

* 返回 `OK`：插入成功
* 返回 `ERROR`：插入失败（以下任一情况）

  * `L == NULL`
  * 顺序表已满（`length >= capacity`）
  * `pos < 1` 或 `pos > length + 1`

功能要求：

* 插入位置说明（1-based）：

  * `pos = 1`：插入到表头
  * `pos = length + 1`：插入到表尾（等价于尾插）
  * 其它：插入到中间
* 插入成功时需要：

  * 将 `pos` 及其后的元素整体向后移动一位
  * 将 `x` 放入 `data[pos-1]`
  * `length` 自增 1
* 插入失败时：

  * 不修改顺序表内容与 `length`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 提示

【位置说明】

* `pos` 为 **1-based**
* 合法范围：`1 ≤ pos ≤ length + 1`

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 头插、尾插、中间插入
  * 空表插入（`pos = 1`）
  * 非法位置：`pos = 0`、`pos = length + 2`
  * 满表插入失败

【提交说明】

* 只需提交 `InsertAt` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

Status InsertAt(SeqList* L, int pos, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据
     * 2. 多次调用 InsertAt（含头插/尾插/中间/非法位置）
     * 3. 输出插入后的表内容与长度
     * 4. 清理资源
     */
    return 0;
}
```

---

## 3691-删除指定位置

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现指定位置删除操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **DeleteAt**：删除顺序表第 `pos` 个位置的元素。

---

## 函数说明

函数原型：

```c
typedef enum { OK = 1, ERROR = 0 } Status;

Status DeleteAt(SeqList* L, int pos, ElementType* deleted);
```

参数说明：

* `L`：顺序表指针
* `pos`：删除位置（**1-based**）
* `deleted`：用于保存被删除的元素值（输出参数）

返回值说明：

* 返回 `OK`：删除成功，被删除元素写入 `*deleted`
* 返回 `ERROR`：删除失败（以下任一情况）

  * `L == NULL`
  * `deleted == NULL`
  * `pos < 1` 或 `pos > length`

功能要求：

* 删除成功时：

  * 记录 `data[pos-1]` 到 `*deleted`
  * 将 `pos` 后续元素整体向前移动一位
  * `length` 自减 1
* 删除失败时：

  * 不修改顺序表内容与 `length`
  * 不修改 `*deleted`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【位置说明】

* `pos` 为 **1-based**
* 合法范围：`1 ≤ pos ≤ length`

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 头删、尾删、中间删除
  * 删除后元素左移验证
  * 非法位置：`pos = 0`、`pos = length + 1`
  * 空表删除失败

【提交说明】

* 只需提交 `DeleteAt` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

Status DeleteAt(SeqList* L, int pos, ElementType* deleted);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据
     * 2. 多次调用 DeleteAt（含头删/尾删/中间/非法位置）
     * 3. 输出删除后的表内容与长度
     */
    return 0;
}
```

---

## 3692-删除所有指定值

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现删除所有指定值的操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **RemoveAll**：删除顺序表中所有等于 `x` 的元素，并返回删除的数量。

---

## 函数说明

函数原型：

```c
int RemoveAll(SeqList* L, ElementType x);
```

参数说明：

* `L`：顺序表指针
* `x`：要删除的目标值

返回值说明：

* 返回删除的元素个数
* 若 `L == NULL`，返回 `0`

功能要求：

* 删除后剩余元素的相对顺序保持不变
* 允许使用“覆盖写入”的方式实现（推荐）
* 删除完成后更新 `length`

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 删除值不存在（删除数量为 0）
  * 删除值出现一次/多次
  * 删除后为空表
  * 含负数、重复值的情况

【提交说明】

* 只需提交 `RemoveAll` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

int RemoveAll(SeqList* L, ElementType x);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据（可能含重复元素）
     * 2. 调用 RemoveAll 删除指定值
     * 3. 输出删除数量、删除后的表内容与长度
     */
    return 0;
}
```

---

## 3693-逆置顺序表

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现逆置操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **Reverse**：将顺序表中元素原地逆置。

---

## 函数说明

函数原型：

```c
void Reverse(SeqList* L);
```

参数说明：

* `L`：顺序表指针

返回值说明：

* 无返回值

功能要求：

* 当 `L == NULL` 时不做任何操作
* 当 `L != NULL` 时：

  * 原地逆置 `data[0] ~ data[length-1]`
  * 不允许使用额外顺序表存储（允许使用一个临时变量交换）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 空表逆置
  * 单元素逆置
  * 多元素逆置（奇数/偶数长度）
  * 逆置两次应恢复原顺序

【提交说明】

* 只需提交 `Reverse` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

void Reverse(SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据
     * 2. 多次调用 Reverse（验证可逆性）
     * 3. 输出逆置后的表内容
     */
    return 0;
}
```

---

## 3694-清空顺序表

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现清空操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **Clear**：清空顺序表中所有元素。

---

## 函数说明

函数原型：

```c
void Clear(SeqList* L);
```

参数说明：

* `L`：顺序表指针

返回值说明：

* 无返回值

功能要求：

* 当 `L == NULL` 时不做任何操作
* 当 `L != NULL` 时：

  * 将 `length` 置为 `0`
  * 不需要清除数组内容（测试只看 `length` 与输出效果）

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【数据范围】

* `MAXSIZE = 1000`
* 测试会包含：

  * 非空表清空
  * 空表清空
  * 清空后判空/长度检查
  * 清空后继续插入

【提交说明】

* 只需提交 `Clear` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

void Clear(SeqList* L);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 构造顺序表数据
     * 2. 调用 Clear 清空顺序表
     * 3. 输出清空后的表内容与长度
     * 4. 清空后再次插入验证可用性
     */
    return 0;
}
```

---

## 3695-销毁顺序表

### 问题描述

本题在“固定容量顺序表（静态数组）”数据结构上，实现销毁操作。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

请实现函数 **DestroyList**：释放顺序表占用的内存资源，并将指针置空。

---

## 函数说明

函数原型：

```c
void DestroyList(SeqList** pL);
```

参数说明：

* `pL`：顺序表指针的地址（用于销毁后置空）

返回值说明：

* 无返回值

功能要求：

* 当 `pL == NULL` 时不做任何操作
* 当 `pL != NULL` 时：

  * 若 `*pL != NULL`：

    * 释放 `*pL` 指向的内存（使用 `free`）
    * 将 `*pL` 置为 `NULL`
  * 若 `*pL == NULL`：不做任何操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须释放内存并置空指针，避免内存泄漏与野指针问题。
* 只释放顺序表结构体本身即可（内部数组是静态数组，不需要单独释放）。

【数据范围】

* 测试会包含：

  * 正常销毁
  * 重复销毁（销毁 NULL 指针或已销毁的表）
  * `pL == NULL` 的情况

【提交说明】

* 只需提交 `DestroyList` 函数的实现。
* 不要提交 `main`，不要重复定义结构体/宏/其它函数。

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

void DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1. 创建并使用顺序表
     * 2. 调用 DestroyList 销毁顺序表
     * 3. 验证指针是否被置空（输出 1/0）
     */
    return 0;
}
```

---

## 3696-区间删除

### 问题描述

本题要求你实现一个**固定容量顺序表（静态数组）**，并在此基础上完成**区间删除**功能。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;
```

给定整数区间 `[low, high]`（包含端点），删除顺序表中所有满足 `low ≤ 元素值 ≤ high` 的元素，并保持其余元素相对顺序不变。

你需要实现顺序表的**创建、尾插、输出、区间删除、销毁**等功能，使测试程序可以按固定流程完成：

**创建 → 插入 → 输出 → 区间删除 → 输出 → 再插入 → 再区间删除 → 输出 → 销毁**

---

## 函数说明

你需要实现以下 5 个函数：

1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

* 返回新建顺序表指针；申请失败返回 `NULL`
* 创建后必须满足：`length == 0`，`capacity == MAXSIZE`

2）表尾插入

```c
typedef enum { OK = 1, ERROR = 0 } Status;
Status PushBack(SeqList* L, ElementType x);
```

* 成功：将 `x` 插入表尾，`length++`，返回 `OK`
* 失败：`L == NULL` 或满表（`length >= capacity`），返回 `ERROR`，且不修改表

3）输出顺序表

```c
void PrintList(const SeqList* L);
```

* `L == NULL`：输出空行
* `length == 0`：输出空行
* 否则输出 `data[0]~data[length-1]`，元素之间一个空格，行末换行（不多空格）

4）区间删除（本题核心）

```c
int RemoveRange(SeqList* L, ElementType low, ElementType high);
```

* 删除所有满足 `low <= data[i] <= high` 的元素
* 保持剩余元素相对顺序不变
* 返回被删除元素个数
* `L == NULL` 返回 `0`
* 若 `low > high` 视为删除 0 个元素

5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

* 释放 `*pL` 指向内存（使用 `free`）
* 将 `*pL` 置为 `NULL`
* `pL == NULL` 或 `*pL == NULL` 时不做任何操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* `RemoveRange` 推荐使用“覆盖写入”实现（O(n)）。

【数据范围】

* `MAXSIZE = 1000`
* 元素值、`low`、`high` 均为 `int`
* 测试会覆盖：删除 0 个/部分/全部、`low > high`、负数、重复值、接近满表等

【提交说明】

* 只需提交 5 个函数：`InitList / PushBack / PrintList / RemoveRange / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
int      RemoveRange(SeqList* L, ElementType low, ElementType high);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3697-有合并序

### 问题描述

本题要求你实现一个**固定容量顺序表（静态数组）**，并在此基础上完成 **合并两个非降序顺序表** 的综合功能。

已知两个顺序表 `A` 与 `B` 中的元素均按\*\*非降序（允许相等）\*\*排列。请将它们合并到顺序表 `C` 中，使 `C` 仍保持非降序，并包含 `A` 与 `B` 的全部元素（允许重复）。

顺序表类型定义如下：

```c
typedef int ElementType;

#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

#### 函数说明

你需要实现以下 **5 个函数**（基础接口 4 个 + 核心函数 1 个）：

1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

* 成功：返回新建顺序表指针，并满足：`length == 0`，`capacity == MAXSIZE`
* 失败：申请失败返回 `NULL`

2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

* 成功：尾插 `x`，`length++`，返回 `OK`
* 失败：`L == NULL` 或满表（`length >= capacity`），返回 `ERROR`，且**不修改表**

3）输出顺序表

```c
void PrintList(const SeqList* L);
```

* `L == NULL`：输出空行
* `length == 0`：输出空行
* 否则输出 `data[0] ~ data[length-1]`，元素之间一个空格，行末换行（**不多空格**）

4）合并两个有序顺序表（本题核心）

```c
Status MergeSortedList(const SeqList* A, const SeqList* B, SeqList* C);
```

返回值与要求：

* 返回 `OK`：合并成功，`C` 为非降序，且 `C->length == A->length + B->length`
* 返回 `ERROR`：以下任一情况视为失败

  * `A == NULL` 或 `B == NULL` 或 `C == NULL`
  * `A->length + B->length > C->capacity`
* **重要：失败时必须保证 `C` 不被修改**（包括 `C->length` 与 `C->data` 内容保持原样）
* 禁止做法：不允许“先把元素全部放进 C 再排序”（评测会按过程覆盖与边界设计防直解）

5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

* `pL == NULL` 或 `*pL == NULL`：不做任何操作
* 否则：`free(*pL)` 并将 `*pL = NULL`（允许重复销毁，避免野指针）

### 输入

输入由测试代码完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* `MergeSortedList` 推荐使用“两指针归并”实现（时间复杂度 `O(|A|+|B|)`）。
* `A`、`B` 输入保证为**非降序**（允许相等），合并后 `C` 也必须保持非降序。
* 若合并失败（如容量不足或任一指针为 `NULL`），**必须返回 `ERROR` 且不得修改 `C`**（包括 `C->length` 和 `C->data`）。

【数据范围】

* `MAXSIZE = 1000`
* 元素值均为 `int`，可能包含负数与重复值
* 测试会覆盖：`A` 或 `B` 为空表、重复值、负数、`A+B` 接近/超过容量、第二次合并失败时 `C` 不被修改等场景

【提交说明】

* 只需提交 5 个函数：`InitList / PushBack / PrintList / MergeSortedList / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   MergeSortedList(const SeqList* A, const SeqList* B, SeqList* C);
void     DestroyList(SeqList** pL);

int main() {
    /*
     * 此处代码由测试程序自动添加，主要用于：
     * 1) 创建 A/B/C
     * 2) 向 A/B 尾插数据（保证其为非降序）
     * 3) 多次调用 MergeSortedList 覆盖边界
     * 4) 输出中间状态用于验证
     * 5) 销毁并验证指针置空
     */
    return 0;
}
```

---

## 3698-去重

### 问题描述

本题要求你实现一个固定容量顺序表（静态数组），并在此基础上完成 **去重（保留首次出现）** 功能。
给定顺序表 `L`，删除其中所有重复元素，使得每个值只保留**第一次出现**的位置，其余重复出现的元素全部删除。
删除后，剩余元素的相对顺序保持不变（稳定）。
顺序表类型定义如下：
```c
typedef int ElementType;
#define MAXSIZE 1000
typedef struct {
 ElementType data[MAXSIZE]; // 固定容量存储
 int length; // 当前长度
 int capacity; // 容量（应等于 MAXSIZE）
 } SeqList;
 ```
 
 你需要实现顺序表的创建、尾插、输出、去重、销毁等功能，使测试程序可以按固定流程完成：
 
 创建 → 插入 → 输出 → 去重 → 输出 → 再插入 → 再去重 → 输出 → 销毁（并重复销毁验证安全）
 
 ---
 
 ### 函数说明
 
 你需要实现以下 5 个函数：
 
 #### 1）创建并初始化顺序表
 
 ```c
 
 SeqList* InitList(void);
 ```
 
 * 返回新建顺序表指针；申请失败返回 `NULL`
 * 创建后必须满足：`length == 0`，`capacity == MAXSIZE`
 
 #### 2）表尾插入
 
 ```c
 typedef enum { OK = 1, ERROR = 0 } Status;
 Status PushBack(SeqList* L, ElementType x);
 ```
 
 * 成功：将 `x` 插入表尾，`length++`，返回 `OK`
 * 失败：`L == NULL` 或满表（`length &gt;= capacity`），返回 `ERROR`，且不修改表
 
 #### 3）输出顺序表
 
 ```c 
void PrintList(const SeqList* L);
```

* `L == NULL`：输出空行
* `length == 0`：输出空行
* 否则输出 `data[0]~data[length-1]`，元素之间一个空格，行末换行（不多空格）

#### 4）去重（本题核心）

```c
int UniqueKeepFirst(SeqList* L);
```

* 删除所有重复元素，仅保留每个值第一次出现的那个
* 必须保持稳定性（剩余元素相对顺序不变）
* 返回被删除的元素个数
* `L == NULL` 返回 `0`
* 不要求最优复杂度，允许 `O(n^2)` 实现

#### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

* 释放 `*pL` 指向内存（使用 `free`）
* 将 `*pL` 置为 `NULL`
 * `pL == NULL` 或 `*pL == NULL` 时不做任何操作

### 输入

本题的输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题的输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* 必须使用顺序表的“删除 + 移动/覆盖写入”逻辑完成去重。
* 不允许直接使用 `set/unordered_set` 等 STL 容器一键去重。

【数据范围】

* `MAXSIZE = 1000`
* 元素值为 `int`，可能包含负数与重复值
* 测试会包含：全不重复 / 全部相同 / 重复分散出现 / 含负数 / 接近满表 / 多次去重调用

【提交说明】

* 只需提交 5 个函数：`InitList / PushBack / PrintList / UniqueKeepFirst / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include<bits/stdc++.h>
using namespace std;

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
int      UniqueKeepFirst(SeqList* L);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---

## 3699-循环右移

### 问题描述

本题要求你实现一个固定容量顺序表（静态数组），并在此基础上完成 **循环右移 K 位** 的操作，同时提供 **判空** 与 **取长度** 的基础查询接口。

给定顺序表 `L` 和非负整数 `k`，将 `L` 中元素做循环右移：右移 1 位表示最后一个元素移动到表头，其余元素整体后移 1 位。右移 `k` 位等价于右移 `k % length` 位。

顺序表类型定义如下：

```c
typedef int ElementType;

#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

测试程序会按固定流程完成：

创建 → 插入 → 输出 → 右移 → 输出 → 再右移 → 输出 → 再插入 → 输出 → 再右移 → 输出 → 销毁（并重复销毁验证安全）

---

## 函数说明

你需要实现以下 7 个函数：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

* 返回新建顺序表指针；申请失败返回 `NULL`
* 创建后必须满足：`length == 0`，`capacity == MAXSIZE`

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

* 成功：将 `x` 插入表尾，`length++`，返回 `OK`
* 失败：`L == NULL` 或满表（`length >= capacity`），返回 `ERROR`，且不修改表

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

* `L == NULL`：输出空行
* `length == 0`：输出空行
* 否则输出 `data[0]~data[length-1]`，元素之间一个空格，行末换行（不多空格）

### 4）判空

```c
int IsEmpty(const SeqList* L);
```

* `L == NULL` 或 `length == 0` 返回 `1`
* 否则返回 `0`

### 5）取长度

```c
int GetLength(const SeqList* L);
```

* `L == NULL` 返回 `0`
* 否则返回 `L->length`

### 6）循环右移 K 位（本题核心）

```c
void RotateRight(SeqList* L, int k);
```

* 将 `L` 循环右移 `k` 位
* 若 `L == NULL`：不做任何操作
* 若 `length <= 1`：不做任何操作
* `k` 为非负整数，右移位数等价于 `k % length`

### 7）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

* 释放 `*pL` 指向内存（使用 `free`）
* 将 `*pL` 置为 `NULL`
* `pL == NULL` 或 `*pL == NULL` 时不做任何操作（支持重复销毁）

### 输入

本题输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* `RotateRight` 推荐使用“三次逆置”或等价的原地算法实现。
* 必须正确处理边界：空表、单元素表、`k = 0`、`k` 远大于 `length`。

【数据范围】

* `MAXSIZE = 1000`
* 元素值为 `int`，可能包含负数与重复值
* 测试会覆盖：空表右移、`k` 很大、`k` 为 `length` 的倍数、右移后继续插入、重复销毁等

【提交说明】

* 只需提交 7 个函数：`InitList / PushBack / PrintList / IsEmpty / GetLength / RotateRight / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
	ElementType data[MAXSIZE];
	int length;
	int capacity; // should be MAXSIZE
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的函数（共7个） */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
int      IsEmpty(const SeqList* L);
int      GetLength(const SeqList* L);
void     RotateRight(SeqList* L, int k);
void     DestroyList(SeqList** pL);

int main()
{
	/*
	* 此处代码由测试程序自动添加。
	* 你只需提交上述 7 个函数的实现。
	*/
	return 0;
}
```

---

## 3700-批处理操作模拟

### 问题描述

本题要求你实现一个固定容量顺序表（静态数组），并在此基础上完成“批处理操作模拟”。

测试程序会先创建顺序表并尾插一批初始数据，然后执行 `q` 条整数指令。每条指令都会对顺序表进行一次操作（插入/删除/查找/取值/输出），并对某些指令输出结果。最后再额外直接调用若干基础操作函数进行验证，并完成销毁与重复销毁安全检查。

顺序表类型定义如下：

```c
typedef int ElementType;

#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
static const int FAIL_ELEM = (-2147483647 - 1); // INT_MIN
```

---

## 指令定义（整数 opcode）

每条指令均为三元组 `(cmd, a, b)`：

* `cmd = 1`：`push a`
  含义：在表尾插入元素 `a`（忽略 `b`）

* `cmd = 2`：`insert a b`
  含义：在位置 `a`（1-based）插入元素 `b`

* `cmd = 3`：`delete a`
  含义：删除位置 `a`（1-based）的元素（忽略 `b`），并输出被删除值；若失败输出 `FAIL_ELEM`

* `cmd = 4`：`find a`
  含义：查找值 `a` 首次出现的位置（1-based），不存在输出 `-1`（忽略 `b`）

* `cmd = 5`：`get a`
  含义：获取位置 `a`（1-based）的元素并输出；若失败输出 `FAIL_ELEM`（忽略 `b`）

* `cmd = 6`：`print`
  含义：输出当前表内容（忽略 `a,b`）

其中 `FAIL_ELEM = INT_MIN`（即 `-2147483648`），用于表示删除/取值失败时的输出占位。

---

## 函数说明

你需要实现以下 9 个函数（基础接口 + A类操作 + 1 个核心综合函数）：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

* 成功：返回新建顺序表指针，并满足 `length == 0`，`capacity == MAXSIZE`
* 失败：申请失败返回 `NULL`

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

* 成功：尾插 `x`，`length++`，返回 `OK`
* 失败：`L == NULL` 或满表，返回 `ERROR`，且不修改表

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

* `L == NULL` 或 `length == 0`：输出空行
* 否则输出元素，空格分隔，行末换行

### 4）按位置插入（1-based）

```c
Status InsertAt(SeqList* L, int pos1, ElementType x);
```

* 合法位置：`1 .. length+1`
* 成功：插入并 `length++`，返回 `OK`
* 失败：`L == NULL` 或满表或位置非法，返回 `ERROR` 且不修改表

### 5）按位置删除（1-based）

```c
Status DeleteAt(SeqList* L, int pos1, ElementType* deleted);
```

* 合法位置：`1 .. length`
* 成功：删除并 `length--`，通过 `deleted` 返回被删值，返回 `OK`
* 失败：参数非法/位置非法，返回 `ERROR` 且不修改表

### 6）按位置取值（1-based）

```c
Status GetElem(const SeqList* L, int pos1, ElementType* out);
```

* 合法位置：`1 .. length`
* 成功：通过 `out` 返回元素值，返回 `OK`
* 失败：参数非法/位置非法，返回 `ERROR`

### 7）按值查找（1-based）

```c
int LocateElem(const SeqList* L, ElementType x);
```

* 返回 `x` 首次出现的位置（1-based）
* 不存在返回 `-1`
* `L：NULL` 返回 `-1`

### 8）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

* 释放 `*pL` 指向内存（free）
* 将 `*pL` 置为 `NULL`
* `pL == NULL` 或 `*pL == NULL` 时不做任何操作（支持重复销毁）

### 9）执行单条指令（本题核心）

```c
Status ExecuteCommand(SeqList* L, int cmd, int a, int b, int* out_int, ElementType* out_elem);
```

功能要求：

* 根据 `(cmd,a,b)` 对 `L` 执行一次操作
* 返回 `OK/ERROR` 表示该操作是否成功（非法指令也视为 `ERROR`）
* 对于会产生输出的指令：

  * `cmd=4`（find）：将结果位置写入 `*out_int`（未找到写 `-1`）
  * `cmd=3`（delete）与 `cmd=5`（get）：将结果写入 `*out_elem`；失败时可写 `FAIL_ELEM`
* `L == NULL` 时直接返回 `ERROR`

### 输入

本题输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* `InsertAt/DeleteAt/GetElem/LocateElem` 必须按接口定义正确处理 1-based 位置与非法情况。
* `ExecuteCommand` 必须通过调用上述接口完成指令执行（不建议把全部逻辑硬编码在一个函数里）。
* 删除与插入必须通过“移动/覆盖写入”维护顺序表存储。

【数据范围】

* `MAXSIZE = 1000`
* 元素值与指令参数均为 `int`
* 测试会覆盖：空表操作、非法位置（≤0 或 >length 范围）、重复值查找、接近满表插入失败、删除/取值失败输出 `FAIL_ELEM`、多轮操作后长度一致性等

【提交说明】

* 只需提交 9 个函数：
  `InitList / PushBack / PrintList / InsertAt / DeleteAt / GetElem / LocateElem / ExecuteCommand / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity;
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
static const int FAIL_ELEM = (-2147483647 - 1); // INT_MIN

/*
需要你实现的函数（共9个）：
- 4个基础接口：InitList / PushBack / PrintList / DestroyList
- 4个A类操作：InsertAt / DeleteAt / GetElem / LocateElem
- 1个核心综合函数：ExecuteCommand（批处理单步执行）
*/

SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
void     DestroyList(SeqList** pL);

Status   InsertAt(SeqList* L, int pos1, ElementType x);         // 1..len+1
Status   DeleteAt(SeqList* L, int pos1, ElementType* deleted);  // 1..len
Status   GetElem(const SeqList* L, int pos1, ElementType* out);  // 1..len
int      LocateElem(const SeqList* L, ElementType x);            // 1..len, not found:-1

/*
cmd 定义（整数，避免字符串解析）：
1 x 0    : push x
2 pos x  : insert pos x
3 pos 0  : delete pos          -> 若成功输出被删元素，否则输出 FAIL_ELEM
4 x 0    : find x              -> 输出位置或 -1
5 pos 0  : get pos             -> 若成功输出元素，否则输出 FAIL_ELEM
6 0 0    : print               -> 输出当前表（空表输出空行）

ExecuteCommand：
- 成功返回 OK，失败返回 ERROR
- find 的结果写到 out_int（失败也可写 -1）
- delete/get 的结果写到 out_elem（失败写 FAIL_ELEM）
*/
Status ExecuteCommand(SeqList* L, int cmd, int a, int b, int* out_int, ElementType* out_elem);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 9 个函数的实现。
     */
    return 0;
}
```

---

## 3701-有序原地去重

### 问题描述

本题要求你实现一个固定容量顺序表（静态数组），并在此基础上完成**有序表原地去重**。

给定一个非降序（允许相等）的顺序表 L，删除其中所有重复元素，使得每个值在表中只出现一次，并保持剩余元素的相对顺序不变。要求原地完成（在原表上修改）。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];  // 固定容量存储
    int length;                 // 当前长度
    int capacity;               // 容量（应等于 MAXSIZE）
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

---

## 函数说明

你需要实现以下 5 个函数：

### 1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

- 返回新建顺序表指针；申请失败返回 NULL
- 创建后必须满足：`length == 0`，`capacity == MAXSIZE`

### 2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

- **成功**：将 x 插入表尾，`length++`，返回 OK
- **失败**：`L == NULL` 或满表（`length >= capacity`），返回 ERROR，且不修改表

### 3）输出顺序表

```c
void PrintList(const SeqList* L);
```

- `L == NULL` 或 `length == 0`：输出空行
- 否则输出 `data[0]~data[length-1]`，元素之间一个空格，行末换行（不多空格）

### 4）有序表去重（本题核心）

```c
int UniqueKeepFirst(SeqList* L);
```

- L 保证为非降序
- 删除所有重复元素，使每个值只保留一次
- 返回被删除的元素个数
- `L == NULL` 返回 0

### 5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

- 释放 `*pL` 指向内存（使用 `free`）
- 将 `*pL` 置为 NULL
- `pL == NULL` 或 `*pL == NULL` 时不做任何操作（支持重复销毁）

### 输入

本题输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题输出数据由测试代码自动完成，你无需编写输出处理。

### 【实现要求】

- 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
- `UniqueKeepFirst` 推荐使用"双指针覆盖写入"实现（时间复杂度 O(n)）。
- 只需修改顺序表结构体本身（内部数组是静态数组，不需要单独释放）。

### 【数据范围】

- `MAXSIZE = 1000`
- 元素值为 int，可能包含负数与重复值
- 输入保证每次插入后 L 仍为非降序
- 测试会覆盖：空表、全相同、全不重复、重复成段出现、接近满表、多次去重调用等

### 【提交说明】

- 只需提交 5 个函数：`InitList` / `PushBack` / `PrintList` / `UniqueKeepFirst` / `DestroyList`
- 不要提交 main，不要重复定义结构体/宏/其它函数
- 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
	ElementType data[MAXSIZE];
	int length;
	int capacity; // should be MAXSIZE
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
int      UniqueKeepFirst(SeqList* L);
void     DestroyList(SeqList** pL);

int main()
{
	/*
	* 此处代码由测试程序自动添加。
	* 你只需提交上述 5 个函数的实现。
	*/
	return 0;
}
```

---

## 3702-有序合并去重

### 问题描述

本题要求你实现固定容量顺序表（静态数组），并在此基础上完成 **两个有序表的并集（去重）**。

给定两个非降序顺序表 `A`、`B`（允许重复），生成 `C` 为它们的并集：

* `C` 为非降序
* `C` 中每个值只出现一次（集合意义上的并集）

若容量不足或参数非法，操作失败：必须返回 `ERROR` 且 **不得修改 `C`**（包括 `C->length` 与 `C->data`）。

顺序表类型定义如下：

```c
typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE]; // 固定容量存储
    int length;                // 当前长度
    int capacity;              // 容量（应等于 MAXSIZE）
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;
```

测试程序会按固定流程完成：

创建 A/B/C → 插入 A/B → 输出 → 求并集到 C → 输出 → 再向 A/B 追加（仍非降序）→ 输出 → 重建 C 并放入哨兵值 → 再求并集（可能失败）→ 输出 → 销毁（并重复销毁验证安全）

---

### 函数说明

你需要实现以下 5 个函数：

1）创建并初始化顺序表

```c
SeqList* InitList(void);
```

2）表尾插入

```c
Status PushBack(SeqList* L, ElementType x);
```

3）输出顺序表

```c
void PrintList(const SeqList* L);
```

4）并集（去重）生成新表（本题核心）

```c
Status UnionSortedUnique(const SeqList* A, const SeqList* B, SeqList* C);
```

功能要求：

* `A`、`B` 非降序，允许重复
* 生成 `C` 为 `A ∪ B`（集合并集），`C` 非降序且无重复
* 成功返回 `OK`
* 失败条件（任一满足即失败）：

  * `A == NULL` 或 `B == NULL` 或 `C == NULL`
  * 并集元素个数 > `C->capacity`
* 失败时：必须返回 `ERROR` 且 **不得修改 `C`**

5）销毁顺序表

```c
void DestroyList(SeqList** pL);
```

### 输入

本题输入数据由测试代码自动完成，你无需编写输入处理。

### 输出

本题输出数据由测试代码自动完成，你无需编写输出处理。

### 提示

【实现要求】

* 必须使用本题给定的顺序表结构与接口完成任务，不允许使用 STL 容器替代顺序表存储。
* `UnionSortedUnique` 推荐使用“双指针归并 + 去重”实现（时间复杂度 `O(|A|+|B|)`）。
* **失败不修改 C** 是强约束：容量不足或空指针时不得改变 `C->length` 与 `C->data`。

【数据范围】

* `MAXSIZE = 1000`
* 元素值为 `int`，可能包含负数与重复值
* `A`、`B` 输入保证非降序
* 测试会覆盖：空表并集、重复值密集、完全不相交导致并集接近 `|A|+|B|`、容量不足失败、失败不修改 C 等

【提交说明】

* 只需提交 5 个函数：`InitList / PushBack / PrintList / UnionSortedUnique / DestroyList`
* 不要提交 `main`，不要重复定义结构体/宏/其它函数
* 不要输出任何提示性文字（如 PASS/FAIL/ERROR）

### 预置代码

```c
#include <stdio.h>
#include <stdlib.h>

typedef int ElementType;
#define MAXSIZE 1000

typedef struct {
    ElementType data[MAXSIZE];
    int length;
    int capacity; // should be MAXSIZE
} SeqList;

typedef enum { OK = 1, ERROR = 0 } Status;

/* 需要你实现的 5 个函数 */
SeqList* InitList(void);
Status   PushBack(SeqList* L, ElementType x);
void     PrintList(const SeqList* L);
Status   UnionSortedUnique(const SeqList* A, const SeqList* B, SeqList* C);
void     DestroyList(SeqList** pL);

int main()
{
    /*
     * 此处代码由测试程序自动添加。
     * 你只需提交上述 5 个函数的实现。
     */
    return 0;
}
```

---
