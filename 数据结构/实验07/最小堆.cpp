#include <stdio.h>
#include <stdlib.h>

typedef struct {
    int* data;
    int  size;
    int  capacity;
} MinHeap;

MinHeap* CreateMinHeap(int capacity);

int main()
{
    return 0;
}

/******************** 学生提交的函数将被拼接在此处 ********************/
MinHeap* CreateMinHeap(int capacity) {
    
}
